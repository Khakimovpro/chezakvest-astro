{
  "name": "chezakvest-astro",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10"
  },
  "description": "Пиксель-в-пиксель клон главной чезаквест.рф на Astro (демо скорости для переезда с Tilda)",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs",
    "generate:redirects": "node migration/build_legacy_redirects.mjs",
    "verify:redirects": "node migration/build_legacy_redirects.mjs --check",
    "verify:redirect-targets": "node scripts/redirect-target-contract.mjs",
    "verify:seo": "node scripts/seo-data-audit.mjs",
    "verify:assets": "node scripts/asset-audit.mjs",
    "verify:production": "node scripts/production-contract.mjs",
    "verify:github-pages": "SITE_BASE=/chezakvest-preview npm run build && SITE_BASE=/chezakvest-preview npm run verify:assets && SITE_BASE=/chezakvest-preview npm run verify:production && npm run verify:public-preview",
    "verify:public-preview": "node scripts/public-preview-contract.mjs",
    "verify:live": "node scripts/live-smoke.mjs",
    "audit:production": "npm audit --omit=dev --audit-level=high",
    "ci": "npm run verify:redirects && npm run verify:seo && npm run test && npm run build && npm run verify:assets && npm run verify:redirect-targets && npm run verify:production && npm run audit:production && npm run verify:github-pages && npm run build && npm run verify:assets && npm run verify:redirect-targets && npm run verify:production"
  },
  "dependencies": {
    "astro": "^7.2.4"
  },
  "optionalDependencies": {
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
