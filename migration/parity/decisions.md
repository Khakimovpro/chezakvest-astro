# Решения parity-проверки — 11.08.2026

Этот журнал фиксирует автономно принятые решения в ходе проверки переноса живого Tilda-сайта в Astro.

| Время (UTC) | Развилка | Решение | Обоснование |
| --- | --- | --- | --- |
| 2026-08-11 11:26 | Откуда брать список страниц | Строить новое объединение из live `sitemap.xml`, live `robots.txt`, свежего внутреннего обхода, `work/raw/active_hidden_pages.csv` и `migration/legacy-url-map.csv`. | Исторический `work/raw/sitemap-pages.xml` не является валидным sitemap, а живой сайт может меняться. |
| 2026-08-11 11:27 | Что считать маршрутом клона | Брать только свежий `dist/**/index.html`, а не `migration/pages.csv`. | В текущем реестре отсутствует реально собранный `/privacy/`; `dist` — фактический артефакт поставки. |
| 2026-08-11 11:28 | Legacy URL с `301 + fallback` | Считать `redirect_ok`, если в `legacy-url-map.csv` есть постоянный redirect и его target собран. В локальном `http.server` не пытаться выдать fallback за HTTP-301. | Локальный static server не читает `_redirects`; production-контракт и generated redirect artifacts являются корректным доказательством поведения хоста. |
| 2026-08-11 11:29 | Параметры визуальной съёмки | Использовать один браузерный прогон с 1440×900 и 390×844, DPR 2; для мобильного включить touch/mobile emulation. | Это точные параметры задания; прежние скрипты используют другие viewport/DPR. |
| 2026-08-11 11:30 | Шум Tilda и слайдеров | Перед съёмкой отключать animations/transitions, `prefers-reduced-motion` и принудительно оставлять первый слайд. | Иначе сравнение измеряет фазу анимации, а не вёрстку. |
| 2026-08-11 11:31 | Скриншоты и Git | Хранить все пары в `migration/parity/shots/`, но исключить тяжёлые бинарные снимки из Git; коммитить CSV, JSON, код и текстовые отчёты. | Задание требует сохранить снимки локально, но прямо разрешает не коммитить тяжёлые файлы. |
| 2026-08-11 11:43 | SEO-правило против live-источника | Приоритет отдан точным `title`/`description`/`h1` с живого сайта, включая исторические опечатки, пустое description и длины вне общего SEO-эвристического диапазона. | Цель миграции — parity, а не редакционная оптимизация живого содержания. Эвристики остаются обязательными; допускается лишь field-level exception при exact match со свежим committed live snapshot. |
| 2026-08-11 11:47 | Адрес двух площадок в карточке квеста | Сохранить перенос строки `Магнитогорская, 1\n40-летия Победы, 216` в 11 данных квестов. | Original отображает два адреса отдельными строками; пробел склеивал их в clone. |
| 2026-08-11 12:03 | Нормализация главного промо-слайдера | Остановить clone-слайдер и приводить Tilda-слайдер к реальному первому `data-slide-index=1`; локальные баннеры упорядочены по live-карусели. | У Tilda технический index 0 — дубликат последнего слайда; клик по нему не является выбором первого баннера. |
| 2026-08-11 12:04 | Привидение в hero | Воспроизвести разные settled-состояния: скрыть mascot на 1440 px и показать его на 390 px в live-геометрии. | Свежие полные R8-пары показывают, что Tilda desktop оставляет слой скрытым, а мобильная композиция его показывает. Предыдущее предположение «видно в обоих viewport» было опровергнуто снимками и исправлено в source CSS. |
| 2026-08-11 12:06 | Локальная SEO-проверка | Сохранить прежние SEO-эвристики обязательными; исключать конкретное правило только когда точное значение поля совпадает с зафиксированным свежим live-snapshot. | Нельзя ослаблять CI ради parity. Snapshot делает исключение проверяемым: любая последующая правка, не совпадающая с live, снова падает на исходной эвристике. |
| 2026-08-11 12:19 | Каталог на главной | Воспроизвести живой порядок 35 карточек и перенести `Звонок`/`Техасскую резню`/`Оно` из «Пряток» в «Страшные квесты». | Live DOM показывает именно эту последовательность и разбиение; прежняя локальная группировка меняла визуальный порядок и категорию. |
| 2026-08-11 12:31 | Пустое live description у `/strashnye-kvesty/` | Сохранить единственный пробельный `content=" "` из live в source и разрешить его только для этого exact route в production-контракте. | Это точная Tilda-разметка, а не отсутствие тега. Контракт по-прежнему отклоняет пустые/пробельные description на всех остальных маршрутах; добавлен регрессионный тест. |
| 2026-08-11 12:34 | Ложные URL из JavaScript Tilda при обходе | Исключить содержимое `<script>` из attribute-crawl и принимать JS-навигацию только с literal `/` или `http(s)` target. | Regex принимал строковую конкатенацию `location.href = ' + hurl + '` за URL и создавал синтетические dead rows; фикс покрыт тестом и не сокращает реальные HTML links. |
| 2026-08-11 13:21 | Сопоставление Tilda `rec…` с Astro-секциями | Заменить greedy one-to-one Jaccard с порогом `0.045` на order-preserving semantic alignment. Пара допустима только по одинаковой роли header/footer, двум+ специфичным словам heading, трём+ общим словам текста или exact traceable asset id; пустые технические фрагменты не расходуют clone-секцию. | Tilda дробит один визуальный блок на несколько `rec…`, и прежний алгоритм связывал пустой breadcrumb/spacer с неродственным Astro-блоком. Это искусственно завышало missing/height и скрывало настоящий порядок. Порог качества parity не менялся: после alignment всё ещё действуют 90/88 pixel и 10/15 height gates. |
| 2026-08-11 13:21 | Область масок визуальной метрики | До инспекции удалять из controlled capture только exact replacement nodes трёх разрешённых классов: live calendar/fallback ↔ `.prebook`, live Tilda map ↔ `.venues__map`/`.vmap`, live MyReviews ↔ `.reviews`. В `notes` matrix записывать фактически применённые mask kinds. | Это единственные согласованные intentional replacements. В маску не входят footer, callback/chat, bonus, обычные формы, карточки и изображения; после collapse высота и pixel comparison измеряют оставшийся реальный контент без искажений внешними виджетами. |
| 2026-08-11 13:21 | `aria-label` в visual text coverage | Не добавлять `aria-label` в видимый текст при section/text parity. | `aria-label` формирует accessible name и часто описывает SVG/social icon, которого пользователь не видит. Он остаётся в DOM и проверяется функциональными/a11y проверками, но не может создавать ложный visual-text defect (`Написать нам`, `Электронная почта`). |
| 2026-08-11 13:21 | Сопоставление image id после Astro build | Не считать Tilda `tild…` id «пропавшим изображением», если clone отдаёт content-addressed/hashes asset без обратной source-map. Такие id писать как `unmapped source asset ids (not missing proof)`; `missing_images` заполнять лишь когда clone вообще не отрисовал qualifying image. | Hash filename не доказывает отсутствие визуально того же asset. Строгая pixel-проверка семантически сопоставленных секций остаётся источником визуального доказательства; exact Tilda id по обе стороны продолжает использоваться как положительное evidence для matching. |
| 2026-08-11 13:08 | Порядок карточек `/strashnye-kvesty/` | Восстановить порядок live-сетки: после первых трёх карточек идут Дракула, Уэнсдей, Психбольница, затем Кальмар, Оно, Дом призраков, Бегущий, Звонок, Техас, Коралина. | Полная R8-пара показывает этот порядок на обоих viewport; данные клона переставляли девять следующих карточек, хотя набор уже был локально перенесён. |
| 2026-08-11 13:10 | Полная тёмная тема `/patologiya/` | Установить `theme: "dark"` и применить уже подготовленный общий `theme-dark` QuestPage. | Полная пара R8 1440/390 подтверждает тёмный фон, карточки и контраст на всём live-маршруте; светлый clone дал лишь 3.18%/4.16% pixel parity. |
| 2026-08-11 13:10 | Ряды «Другие квесты» | Не отфильтровывать текущую карточку из source-списка; дополнить общий набор Нагибина карточкой «Фантастический угон». | В R8 source `/mystery_shack/`, `/pirati/`, `/pobeg/` содержит шесть карточек, включая текущую; clone скрывал её и не имел шестой. Факт закреплён регрессионным тестом. |
| 2026-08-11 13:22 | Сквозная палитра campaign-лендингов | Для `/prazdnik-maxi/`, `/roblox-land/`, `/amongus-land/`, `/minecraft-lend/`, `/igra-v-kalmara-lend/` и `/vypusknoj-kalmar/` включить отдельную тёмную holiday-тему с палитрами, снятыми с R8 live-пар. | На всех шести original использует тёмный canvas, светлую типографику и тематический акцент; clone ошибочно рендерил светлую бумажную общую тему. `/new-year/` и `/prazdniki-pod-kluch/` оставлены без этого решения: у первого live светлый, у второго live-документ в capture фактически деградировал/пуст. |
| 2026-08-11 13:27 | `/privacy/` как source 200-маршрут | Считать `/privacy/` полноценной live-страницей, а не `extra_clone`; убрать clone Header/breadcrumb/card, перенести exact текст из `work/raw/pages/privacy--8ac334ed5f.html` в локальный статический `src/data/privacy-policy.html`, сохранить source title и пустые description/H1. После текста оставить live-последовательность «Наши площадки» → локальная карта → footer. | Свежий live snapshot фиксирует `200`, `title=Политика обработки персональных данных`, `description=""`, `h1=""`; browser-замер source даёт 960 px copy-column, 105/75 px hero и 60/8/6/60 px policy rhythm на desktop, а на 390 px — 350 px column. Локальный Nunito asset исключает внешний font request; `LazyMap` остаётся уже задокументированным intentional map replacement. |
| 2026-08-11 14:02 | Header и текстовые пары Tilda | Считать source-header только пустой короткий layout-spacer (12–160 px без текста/изображений), а не первый видимый `rec…`; визуальный текст читать из attached `element.innerText`, а не из detached clone. | На `/privacy/` первым `rec…` является semantic hero, поэтому прежнее правило ложным образом создавало missing/extra header; после исправления нет отсутствующих/лишних semantic sections, геометрия 0.76% desktop и 0.07% mobile. Detached clone подмешивал hidden Tilda form-schema из textarea (`li_name`, Unicode-escapes) и не давал сопоставить source callback `/ono/` с clone; rendered copy даёт semantic pair. Пороги pixel/height и содержимое capture не ослаблялись. |
| 2026-08-11 14:12 | Геометрия hero campaign-страниц | Сделать параметры hero частью данных (`heightDesktop`, `heightMobile`, `fullBleed`, `hideSharedHeader`, `copyPosition`), а не route-specific CSS. | R9 source фиксирует разные canvas: `/amongus-land/` 900/844 px, `/roblox-land/` 938/918 px, `/igra-v-kalmara-lend/` 856/770 px, `/vypusknoj-kalmar/` 834/748 px, `/new-year/` 688/767 px и `/prazdniki-pod-kluch/` 910/1100 px (desktop/mobile). Общая qhero-карта меняла как высоту, так и положение первого смыслового блока. |
| 2026-08-11 14:12 | Mobile-карусель залов и callback в двух кампаниях | На `/igra-v-kalmara-lend/` и `/vypusknoj-kalmar/` оставить на mobile первый видимый кадр carousel и убрать общий CallbackForm. | В R9 Tilda показывает только первый hall-card в захваченном viewport; рендер всех карточек подряд искусственно увеличивал секцию. В обоих source-документах после venue/footer нет общего callback-блока. |
| 2026-08-11 14:12 | Тёмная category-страница `/strashnye-kvesty/` | Воспроизвести source order hero: верхний текст, outline-CTA «Рассчитать стоимость праздника», затем primary «Подобрать квест»; скрыть общий Header/reviews и сохранить source card copy. | R9 1440/390 показывает 90 px технического верхнего spacer вместо shared Header, stack из двух кнопок и отсутствие локального reviews-widget. Точный live text включает историческую строку «Хостел. Подвал выток», поэтому она не исправляется редакционно. |
| 2026-08-11 14:12 | Недостающие bespoke-art assets | Не подменять отсутствующие Tilda campaign-illustrations произвольными локальными фото/CSS; передать exact ID для зеркалирования отдельному asset-проходу. | R9 pixel residuals у `/minecraft-lend/`, `/amongus-land/`, `/roblox-land/`, `/igra-v-kalmara-lend/`, `/vypusknoj-kalmar/` обусловлены source artboard-слоями, которых нет в local asset registry. Геометрия и semantic copy исправлены здесь, но art parity требует идентичных bitmap. |
| 2026-08-11 14:22 | Главная: порядок карточек и геометрия каталога | Оставить `site.cards` в source-порядке без отдельной `liveHomeOrder`-сортировки; вернуть «Оно», «Техасскую резню бензопилой» и «Звонок» в rail «Прятки в лабиринте 200м²»; применить изолированную `home.css` для измеренной геометрии 1440/390 и source-переносов адресов. | Полная R10-пара и Tilda-artboards фиксируют 15 classic → 3 scary → 6 hide → 8 show → 3 VR; три спорные карточки находятся во втором ряду `rec1630332641`. Их ранее принятое перемещение в «Страшные» опровергнуто свежей полной съёмкой. Source задаёт 360 px cards/1160 px slider track на 1440 и 320 px cards/full-390 px slider на mobile; clone-only pause control отсутствует в original. |
| 2026-08-11 14:28 | CTA выпускного без существующего anchor | Перенаправить `lead-cta` `/vypusknoj-kalmar/` с несуществующего `#callback` на локальную форму `#prazdnik`. | Fresh source has no standalone callback section; его CTA открывает внешний lead/quiz. В Astro единственная эквивалентная локальная lead-форма — `PartyForm#prazdnik`; это сохраняет действие без битой внутренней ссылки и без внешнего запроса. |
| 2026-08-11 14:29 | Размеры изображения локального lightbox | Указать `width="1600" height="1200"` на скрытом до открытия `.lb__img`. | Изображение заполняется динамически только после клика и CSS продолжает ограничивать его реальным viewport; явные intrinsic dimensions закрывают общий HTML-контракт каждого `<img>` без влияния на геометрию страницы. |
| 2026-08-11 14:49 | Границы known gaps | Генерировать `known-gaps.csv` из фактически применённых `metric masks` финальной visual-matrix: одна URL+section строка на booking/map/reviews; удалить wildcard и телефон из реестра. | В §6 требуются проверяемые ограниченные исключения, а не глобальная категория. Канонический телефон совпадает с live HTML и нормализуется только против renderer calltracking, поэтому не является непреодолимой заменой. |
| 2026-08-11 15:03 | Holiday/Category R11 source pass | Для VR и Azkaban включить route-data тёмные themes, но сохранить source header (live record 58–90 px); убрать отсутствующие в source FAQ, оставить VR без reviews и вставить Azkaban reviews в подтверждённой позиции. Hero CTA теперь может явно выбирать primary action из данных. Во всех holiday routes local callback рендерится до address strip. На `/strashnye-kvesty/` предпочитать явную source crop-обложку category item и отрисовывать source gallery как one-frame scroll rail. | R10 live-пары показывали тёмный VR/Azkaban canvas вместо light-paper clone и source CTA order. Scoped R11 повысил pixel metric VR с 1.89/2.815 до 41.98/43.24 и Azkaban с 2.02/2.195 до 64.60/54.16 (1440/390); runtime defects остались нулевыми. У horror category source реально использует carousel, поэтому full grid не сохраняется лишь ради случайного совпадения общей высоты; residual desktop geometry остаётся честным предметом следующего полного круга. |
| 2026-08-11 15:12 | Порядок callback на generic Quest | Рендерить `CallbackForm` перед `VenuesSection`; для data routes без source `PartyForm` давать этому callback id `prazdnik`, чтобы live hero anchor оставался рабочим. | В R10 `/ono/` source `rec1144740346` стоит перед `Наши площадки`, а clone ставил его после (52 ложных `extra:есть вопрос`). Horror source `rec2328825361` также имеет 480px callback до venues, но hero по-прежнему ссылается на `#prazdnik`; alias на существующем callback сохраняет действие без добавления отсутствующей source формы. |
| 2026-08-11 15:12 | Dark horror Quest variants | Пометить `/hostel-podval-pytok/` как `theme: dark`; исключить source-absent `PartyForm` для Hostel/Patologiya/Shizofreniya; оставить локальный dark paper wash и dark callback. Первый кадр Hostel extra — `/assets/q/e6807a8721.webp`. | R10 source всех трёх routes использует тёмный canvas; у Hostel clone был светлым. Live headings не содержат «У нас вы можете отметить день рождения», поэтому форма искусственно увеличивала страницы. Scoped R11 probe после удаления формы дал height delta Hostel 5.24%/0.76%, Patologiya 2.66%/0.92%, Shizofreniya 3.96%/1.42% (1440/390), при 0 runtime/external defects. |
| 2026-08-11 15:12 | Три T121-записи площадки 40-летия Победы | Хранить 16 карточек в одном local data list, но рендерить по source groups 6/7/3; использовать 360px 3-column desktop grid и 320px one-column mobile grid, with source address uptitles. | R10 source records `rec1630332641`, `rec2350106591`, `rec1634958751` содержат соответственно 6, 7 и 3 cards. Clone имел лишь первые 6, что давало missing text и 41.78%/50.60% height delta. После scoped pass page height delta стал 10.01% desktop и 2.70% mobile; no external assets were introduced. |
| 2026-08-11 15:10 | Главная: bitmap-карточек, hero и copy-box | Для `/` использовать exact live `noroot.png` (1479×891) и 26 Tilda-деривативов `cover/720x720`; сохранить у каждой небэйкнутой карточки измеренные ширины текста (166–232 px desktop, 137–203 px mobile), неразрывные пробелы и пять source `<br>`. | В live DOM Chromium при DPR=2 обе ширины загружают `static.tildacdn…/noroot.png`, а 26 карточек — `optim.tildacdn…/-/cover/720x720/...`; прежние 432px-webp и единый 64% copy-box давали другой crop/перенос при почти совпадающей геометрии grid. CSS-переменные ограничены `.home-page`, поэтому category/quest-card reuse не меняется. |
| 2026-08-11 15:18 | Macro-выравнивание Tilda `rec…` | Добавить исключительно one-to-many (ровно 2 source record → 1 clone section) order-preserving alignment, если records непосредственно соседние, не являются header/footer/widget/breadcrumb, каждая половина имеет 3+ специфичных токена с ≥50%/3 общими с clone (либо traceable exact asset), а объединённый corpus имеет 6+ токенов и ≥70% coverage. Для принятой macro-пары pixel comparison берёт crop полного source-диапазона из full-page screenshots и clone-crop целиком; raw inspection/CSV section counts, masks и 90/88 px + 10/15 height gates не меняются. | R10 показывает реальные title+cards пары (`rec2242726831+rec2243142551`, `rec2094236451+rec2094236461`, `rec1158430236+rec1158430241`), где индивидуальный title ложно создаёт max-section-height. Строгие coverage/adjacency rules не позволяют поглотить `privacy` policy body, отдельные home blocks или Tilda breadcrumb `Главная …`; одно к одному остаётся fallback. |
| 2026-08-11 15:14 | CTA horror-страниц без PartyForm | Передавать `sectionId="prazdnik"` в существующий `CallbackForm`, когда `showPartyForm: false`; оставить его input-id `prazdnik-*`. | После исключения отсутствующей у оригинала PartyForm три исходных hero CTA всё ещё ведут на `#prazdnik`. Ранее менялся только input prefix, а сам section сохранял id `callback`, что production-контракт корректно выявил как три битых ссылки. Теперь CTA ведёт на единственную source-эквивалентную local lead form; покрыто тестом и static contract. |
| 2026-08-11 15:18 | Видимые labels PartyForm | Оставить source-видимые подписи полей, но реализовать их как реальные связанные `<label>`: date — внешняя `Выберите дату`, name и phone — text внутри поля с точной source copy. Для phone добавить `aria-label="Телефон"`. | CSS visually clipped все три label до 1×1 px, хотя форма функциональна. Это нарушало смысл production-контракта и скрывало доступные имена. Новая реализация сохраняет исходный вид пустых полей (`Ваше имя`, `+7 (000) 000-00-00`) без дублирования placeholder и без изменения набора полей; desktop/mobile layout локально скорректирован и защищён тестом. |
| 2026-08-11 15:29 | Общий верхний ритм QuestPage | Для `QuestPage` задать ровно 90 px header, убрать только desktop 14 px верхнего отступа hero и добавить 86 px к breadcrumb spacer; также сместить desktop mega-menu под 80px внутреннюю панель. | Полная R10-инвентаризация 39 generic quest routes подтверждает одинаковые source значения в обоих viewport: header `90` против clone `80`, desktop hero `550` против clone `564`, а от hero до story — `127` против clone `41` px. Изменение scoped к `.quest-page`: Venue/Category/Holiday сохраняют свои отличающиеся измеренные ритмы; mobile hero остаётся 530 px. |

| 2026-08-11 15:29 | Отдельный foreground hero `/strashnye-kvesty/` | Зеркалировать exact transparent WebP `tild3337-3537-4933-a532-356335333533/photo.webp` и рендерить его отдельным локальным hero-layer с Tilda artboard координатами 700/18/380/532 на desktop и 106/138/234/332 на mobile. | Live `rec2221496071` состоит из двух raster layers: уже перенесённого cellar background и независимого персонажа с бензопилой. Использование одного background bitmap теряло крупный видимый объект на обоих required viewports; foreground не является widget/mask и воспроизведён локально без внешнего запроса. |
| 2026-08-11 15:38 | Многослойные campaign-artboards | Для `/amongus-land/`, `/roblox-land/`, `/minecraft-lend/`, `/igra-v-kalmara-lend/`, `/vypusknoj-kalmar/` и `/new-year/` хранить captured Tilda foreground/decor в data-driven `hero.layers` с отдельными desktop/mobile artboard coordinates; HTML рендерит локальные `<img>` выше source background. | R11 диагностирует, что полноэкранный campaign hero теряет не только фон, но и видимых персонажей, brand SVG, blur/decor и band-layers. Каждый подключённый файл зеркалирован из source CDN локально и имеет declarative intrinsic/viewport coordinates; никаких remote refs, CSS-имитаций или widget masks не добавлено. Title/description/H1 сверены с R11 original inspection; историческое source `праздникии` оставлено byte-exact. |
| 2026-08-11 16:16 | Fallback CTA Maxi и SITE_BASE privacy | На `/prazdnik-maxi/` переназначить source Marquiz-popup fallback с удалённого `#prazdnik` на существующий local `#callback`; для privacy собрать URL font/tile из `import.meta.env.BASE_URL` в inline style. | Holiday R13 source убрал несоответствующую PartyForm, поэтому `#prazdnik` стал битым. `#callback` сохраняет эквивалентное lead-действие плюс `data-quiz`. Privacy работала в root, но root-absolute assets ломались бы при `SITE_BASE=/chezakvest-preview`; inline compiled base URL сохраняет exact repeated Tilda tile в обоих deployment modes. Оба случая закрыты регрессионными tests и production/GitHub Pages contracts. |
| 2026-08-11 15:50 | Фоновая плитка `/privacy/` | Для одного `/privacy/` переопределить shared background на exact local source PNG `tild3463-3931-4833-b565-346437376365/_GPT_light.png` с `repeat`, `size:auto`, `position:center`; не менять глобальный layout. | Raw live `rec1097984951` имеет `.t674__cover_repeat` с `background-repeat:repeat`, `background-size:auto`, `background-position:center`, тогда как shared clone rule растягивает WebP на `100% auto`. R11 geometry уже проходит 0.76%/0.07%, а residual pixel 74.49%/75.70%; source-scoped tile rule устраняет подтверждённое расхождение и не создаёт external request. Focused test проверяет PNG signature и selector contract. |

| 2026-08-11 15:56 | Source conversion records and first-frame halls | Make `PartyForm` data-labelled (`title`, `subtitle`, CTA, stable section id) and retain only source-backed conversion records. `/kids/` now has the source quiz before its one visible hall and its later invitation form; `/den-rozhdeniya-uznik-azkabana/` has its quiz and final booking form; `/prazdniki-pod-kluch/` uses its four-question gift calculation copy. Remove VR’s source-absent party form and source-absent generic callbacks on Kids, Azkaban and Pod Kluch. | R11 inspected records identify exact live copy and order: Kids `rec844797097/111/112`, Azkaban `rec209423651/740739`, and Pod Kluch `rec677119234`. The generic "У нас вы можете отметить…" and "Есть вопрос" blocks were comparator extras, rather than source evidence. Distinct IDs keep two local forms on a page valid and preserve `#prazdnik` targets; VR’s former `#prazdnik` CTA now targets its existing source package section (`#pakety`) instead of a removed fake form. |
| 2026-08-11 15:56 | Holiday body residuals | Remove source-absent FAQ/marketing sections from `/kids/` and `/prazdnik-maxi/`; move source-backed reviews into their observed positions; use `10 000` source review count; render only the documented first Tilda hall slide on Kids and Maxi. | R11 records show no FAQ or `Чё за праздник` block, while source has Kids review heading `rec844797129`, Pod Kluch review heading `rec677119229`, and single visible hall records `rec844797103` / `rec2262811631`. The remaining hall data is retained for navigation but not stacked beneath the initial captured carousel frame. |
| 2026-08-11 15:59 | Главная: mobile artboard «Проведите незабываемый праздник» | На ширине ≤479 px заменить компактный shared party layout на измеренный source artboard: 173 px title + 910 px visual panel, пять индивидуально позиционированных stats и exact source button/link `/kids`; desktop shared geometry не менять. | R11 original `rec1513069911` + `rec1161022311` занимает 1 083 px, тогда как clone section — 626 px. В clone mobile static `.party__stats` терял stacking над positioned photo, поэтому пять source-cards исчезали. Raw Tilda artboard даёт все coordinates, panel styling, button asset и target, так что правка source-derived, не threshold/mask workaround. |
| 2026-08-11 16:52 | Responsive LCP asset главной | Для mobile использовать локальный центрированный crop exact `noroot.png` в WebP 780×960 (quality 75, 38,856 B) через `<picture>` и responsive preload; desktop продолжает использовать неизменённый source PNG 1479×891. | Lighthouse mobile выявил PNG 1,238,579 B как LCP resource. Производный файл не создаёт новый контент и не меняет desktop composition: это ровно тот же central `object-fit:cover` crop, который уже виден в 390px hero. Inlining/псевдоэлемент были проверены и отвергнуты, потому что ухудшали FCP и не давали устойчивого выигрыша; выбран обычный локальный responsive asset, а не обход метрики. |
| 2026-08-11 17:12 | Откат responsive LCP asset главной | Не сохранять R14 WebP-кроп: вернуть exact live `noroot.png` на обоих viewport и удалить экспериментальный файл. | Полный R14 показал падение mobile pixel similarity `/` с 47.44% до 43.76%, а Lighthouse оставался ниже цели (best observed 94, повторные runs 90). Производный crop не закрыл performance gate и ухудшил более приоритетную visual parity, поэтому это решение отвергнуто, а итоговый R15 снимается с source asset без замены. |
| 2026-08-11 17:52 | Fallback-группы карточек VenuePage | Для площадок без `games.groups` возвращать единственную группу всего инвентаря; remainder добавлять только после реальных source-групп. Логика вынесена в `src/lib/venue-games.js`. | R15 `/magnitogorskaya1/` отображал 28 clone-карточек вместо 14 source-карточек: fallback сначала добавлял полный список, затем повторно добавлял его как remainder с нулевым offset. Это давало mobile height 14 232 px против 9 810 px у original (+45.08%). Unit-regression проверяет как ungrouped list, так и source-группы 6/7/3 `/40letpobedy216/`. |
| 2026-08-11 17:52 | Приоритет captured art в связанных карточках QuestPage и VenuePage | Считать `item.img` и `item.imgSet` авторитетными; использовать hero linked page только когда у source card нет собственного art. Логика вынесена в `src/lib/card-items.js` и применяется обоими layouts. | R15 `/ono/` показал вместо source thumbnail «Психбольница» hero-кадр спальни: QuestPage безусловно заменял 438 из 439 captured related/scenario thumbnail на image linked route. Та же ошибка оставалась в VenuePage: сохранённый `item.img` сочетался с fallback `imgSet`, поэтому responsive `<img>` снова выбирал hero derivative. Регрессии проверяют оба вида explicit art и fallback без собственного изображения. |
| 2026-08-11 18:25 | Локальные T347/T121-блоки `/ono/` | Добавить в данные `/ono/` opt-in `sourceParity`; только он включает frame видео 560×315 (350×197 mobile, cover) и сценарный rail T121 360px/40px desktop, 280px/10px mobile. | R16 показывает, что native 901×660 poster растягивал video block, а generic 216px square cards не соответствовали captured Tilda `rec1390873221` и `rec1306638331`. Отдельный CSS использует только opt-in classes, поэтому ни `VenuePage`, ни общие Quest routes не меняют геометрию. |
| 2026-08-11 18:34 | T396 зала `/magnitogorskaya1/` | Включить только для `hall.layout: "magnit-t396"` отдельный `MagnitVenueHall`: desktop artboard 165+539 px, rail 600×400, подпись под фото и перекрывающая его полупрозрачная detail-panel; на 390 px — исходная scaled-геометрия 235+755 px. | Прямой live DOM замерил `rec1696439381/rec1696439411`: 1440 — photo `200/4183/600/400`, panel `701/4313/540/341`; 390 — photo `-24.375/7555.67/439/275`, panel `12.1875/7288.70/365.69/318.16`. Общая grid-card `qvenue` не может воспроизвести это наложение. Локальный popup `#popup:zal_magnitogorskaya` отсутствует, поэтому CTA временно сохраняет прежнее рабочее phone-action вместо битого anchor; это не маскирует геометрию и оставляет popup отдельной функциональной задачей. |
| 2026-08-11 18:36 | Первый экран и пакеты `/prazdniki-pod-kluch/` | Для data-opt-in `hero.composition: "podkluch-artboard"` вывести отдельный `PodKluchArtboard`, убрать source-absent breadcrumb и локально зеркалировать exact Tilda PNG: hero layers, три video previews и package cards Standard → Super → Maxi. CTA Tilda popup заменён рабочим локальным `#prazdnik`. | Live/raw `rec684405537` фиксирует artboard 910/1100 px, copy, 3-card video rail и позиции; `rec774459197`/`rec774458892` фиксируют heading и карточки 348×430 (mobile stack 44/587/1130) с CTA «Узнать свободную дату». Общий light hero не содержал ни source composition, ни packages, создавая крупный structural/height residual. Зеркальные bitmap не заменяют содержание и исключают внешний Tilda request; special renderer выбирается только данными этой страницы. |
| 2026-08-11 18:46 | Каноническая R17-метрика `/prazdniki-pod-kluch/` | Считать финальными `migration/parity/visual-matrix-podkluch.csv` и `round-17-podkluch/visual-detail.json`, записанные в 18:46 UTC; исправить ранее озвученное промежуточное `px_390=26.19` на `19.84`. | Два наложившихся targeted-прогона оставили кратковременное промежуточное чтение. Финальные CSV и detail согласованы: R17 `px_1440=36.98`, `px_390=19.84`, heights `14771/8369` и `22175/12860`. Относительно R15 это даёт геометрический выигрыш (height delta `48.90→43.34%`, `49.97→42.01%`), но visual similarity ухудшилась (`39.53→36.98`, `22.885→19.84`); это не следует выдавать за pixel improvement. |
| 2026-08-11 18:46 | Следующий ограниченный Pod-каталог: дополнительные услуги | Следующим isolated source-backed кандидатом считать только связку `rec677119204` + `rec678630258` («Выберите дополнительные услуги» → «Шоу-программы»), не менять общий `htiles` и не захватывать мастер-классы/дополнения. Перед реализацией снять DOM rects/cards на 1440 и 390, зеркалировать восемь exact Tilda bitmap и включить отдельный opt-in renderer лишь для Pod Kluch. | Raw фиксирует source copy, порядок восьми карточек `Нащупал → Мафия → Квиз Баттл → Музыкалити → Распределительная шляпа → Попробуй объяснить → Любимый герой → Ютуб пати`, 4-column T774 desktop container, 8 exact `static.tildacdn.com` bitmap. R17 source-block занимает `164+998=1162 px` desktop и `287+3966=4253 px` mobile, тогда как текущий clone `htiles` — `691/1066 px`; это крупнейший изолированный residual без смешения со следующими catalog records. |
| 2026-08-11 19:01 | T396 зала `/ono/` | Включить только через `sourceParity.hall: "t396"` отдельный `OnoVenueHall`; заменить generic `qvenue` на source artboard `165+570 px` desktop и `235+755 px` mobile. Caption `2 ЗАЛА ПО 30 КВ.М` хранить отдельно от detail-copy; rail остаётся локальным ZoomImg carousel, address link и рабочий phone CTA сохраняются. | Прямой live DOM `rec1390873141/rec1390873151` дал 1440 rail `200/8/600/400`, panel `701/138/540/341`, caption `200/419`, CTA `840/451/300/55`; на 390 — rail `-24.375/415.672/439/275`, panel `12.188/148.703/365.688/353.5`, CTA `36.563/663.125/316.938/56.063`. Generic grid-card не воспроизводит layer order/overlap. Source popup `#popup:zal_pobedy` отсутствует локально, поэтому сохранён ранее рабочий `tel:` CTA, а не добавлен битый anchor. |
| 2026-08-11 19:15 | T131 переключатель двух T396-залов `/ono/` | Передать оба exact local hall data objects только в `OnoVenueHall`; заменить декоративное `1 из 2` на local prev/next buttons, которые без network меняют visible slide, caption, address link и detail copy. Первый кадр/артборд остаётся неизменным; второй адрес ведёт на существующий `/magnitogorskaya1/`. | Raw source `rec1390873191` связывает `#rec1390873151,#rec1390873171` и `#prev/#next`; второй record содержит «улица Магнитогорская, 1», `3 зала по 30 кв.м` и отдельную gallery. Оставлять индикатор `1 из 2` без навигации было функциональным ложным affordance. Existing local Magnit hall assets уже source-backed; не добавлены remote URL или общий `qvenue` behaviour. |

| 2026-08-11 19:04 | Exact assets будущего `PodKluchGameGrid` | Локально зеркалировать 24 primary card thumbnails и 10 source CTA mirror-paths из raw `rec707324259`, `rec707324258`, `rec677181803`, `rec748971024`; связку route/card/path зафиксировать в `migration/parity/podkluch-game-grid-assets.json`. | Это exact `static.tildacdn.com` bitmap, а не linked-page hero fallback. Все 34 public paths существуют, декодируются как изображения; 24 primary имеют собственные bytes, десять исходных `8.png` намеренно одинаковы по SHA-256. Assets подготовлены без изменения data/layout/component и без capture. |
| 2026-08-11 19:06 | T017/T774 дополнительных услуг `/prazdniki-pod-kluch/` | Заменить только generic плитку «Шоу-программы» на route-data `kind: "podkluch-services"` и отдельный `PodKluchServices`; сохранить следующие generic Masterclasses/Additions и не менять shared `htiles`. Зеркальные Tilda popup CTA `#popup:calculatershow` вести на существующий локальный booking `#prazdnik`. | Прямой live DOM `rec677119204/rec678630258` фиксирует intro `163.828/286.984 px` и catalog `997.531/3965.563 px` (1440/390). T774 имеет 4×260 px desktop cards, media ratio `96.153846%`, два ряда с 40 px gap; на 390 — одну колонку, 350 px surface, 336.531 px media, 20 px gap. Все восемь exact bitmap зеркалированы локально, copy/order/«Заказать» остаются source-exact; route-only branch не затрагивает другие holiday routes. |
| 2026-08-11 19:26 | T395/T396 каталог игр `/prazdniki-pod-kluch/` | Заменить три generic `CardsRow` на data-opt-in `podkluch-games` и отдельный `PodKluchGameGrid`: все 24 captured cards, source tab filter, primary+CTA Tilda bitmap, exact desktop/mobile T396 coordinates и локальные links на game routes. Не использовать `canonicalCardItems`, `imgSet` или shared card styles. | Raw `rec679968954`, `rec677181801`, `rec707324259`, `rec707324258`, `rec677181803`, `rec748971024` задаёт heading/tabs и четыре artboard heights `850/490/1640/490` desktop, `1944/1034/3960/1084` mobile. Generic poster rails теряли 15 real cards, source category behavior, source art and fixed positions. Lazyweb report `8f93743c-82a6-4bdb-a4d5-4a98b7fd7232` completed without degraded slots, but raw source remains authoritative for parity rather than its generic design suggestions. |
| 2026-08-11 19:33 | Полный source-artboard `/vypusknoj-kalmar/` | Вывести только для `hero.composition: "vypusknoj-artboard"` отдельный `VypusknojKalmarArtboard`: spacer, T396 hero, intro/features, program, два T923 rails, hall, trust mosaic, R15-masked review rail, gift, venue pills и dark source footer. Generic Holiday sections не рендерить на этой route. Все popup CTA source заменить на локальный `#prazdnik`; `PartyForm` остаётся доступной только по `:target`, поэтому default screenshot не получает отсутствующую у source форму. | R15 — худшая campaign-пара: 1.69/1.385 px при 23 source против 16 generic clone records. R15 raw/visual-detail на 1440/390 фиксирует `rec888951742` 856/770, `rec888951757` 850/1020, `rec892172401` 847/1691, T923 624/563 и 635/722, hall 600/785, trust 770/750, masked records 643/433 и gift 700/490; fresh live DOM подтвердил те же artboards и content, но динамический widget имеет иной runtime height. Зеркалированы exact Tilda hero/feature/program/card/hall/trust/gift bitmaps локально; order show-cards и rails сохранён. Существующие SEO, route links и local lead form сохраняются, external Tilda/Marquiz request не добавлен. Обязательный Lazyweb report завершился без degraded slots: https://www.lazyweb.com/report/lazyweb/64ece76e-eda0-432e-83f1-827e9a7b299b/?source=create; его общие design suggestions не подменяют raw/live источник parity. |
| 2026-08-11 19:36 | T396 программы `/igra-v-kalmara-lend/` | Заменить только generic `packages#program` на data-opt-in `kalmar-programs` и отдельный `KalmarPrograms`; зеркалировать локально фон, три exact PNG-карточки Standard → Super → Maxi и декор `22.png`. Source Marquiz-popup у всех четырёх links заменить на существующую local PartyForm `#prazdnik`; общие holiday package/tiles routes не менять. | Fresh live Tilda `rec866472834/835/838` задаёт dark heading `140/150 px`, H2 `240/47/960/54` (1440) и `46/28/299/105` (390), artboard `638/1549 px`, cards `150|540|930/48` desktop и `15/48|511|974` mobile, CTA `590/529/260×64` и `65/1447/260×56`, а также separator `121/82 px`. Generic text cards не совпадали с baked source artwork. Renderer gated only by target data kind, uses no remote Tilda request, and preserves a working local lead action. |
| 2026-08-11 19:36 | Lazyweb report `/igra-v-kalmara-lend/` | Не выдумывать Lazyweb report и не повторять запрос сверх одного retry: после successful request/upload/resolve current local screenshot `lazyweb_generate_report` дважды вернул HTTP 400 `image_url_fetch_failed`. Продолжить по raw/live Tilda DOM и R15 evidence. | Это повторяемый provider-side fetch failure, зафиксированный после корректной signed-image flow; URL отчёта не был выдан. User-required Lazyweb attempt выполнен, но source parity не может опираться на отсутствующий report. |
| 2026-08-11 19:55 | Interactive local T396 hall galleries | For the isolated `/magnitogorskaya1/` and `/ono/` T396 hall artboards, keep the exact first-frame geometry but make every mirrored source photo reachable through local dot tabs, keyboard navigation and touch swipe; use the source `autoplay=3`, loop, dot colours/sizes and no arrows. The shared helper is `src/scripts/t396-gallery.js`, used only by the two artboards. | Raw `rec1696439411` and `rec1390873171` specify `slds_autoplay=3`, `slds_loop=loop`, `slds_dotscontrols=in`, 8px `#474747/#ff6900` dots and no arrows. Active slides get `aria-hidden`/`inert`; user interaction pauses rotation. The helper honours `prefers-reduced-motion`; parity capture already sets `reducedMotion: 'reduce'`, so controlled captures retain the deterministic first frame. All gallery media and script are local; DOM network smoke found no external requests. Mobile CTA overlap is resolved only through a higher dot stacking layer, without coordinate/size changes. |
| 2026-08-11 19:55 | Magnit hall CTA fallback | Replace the route-local Magnit hall `tel:` fallback with the existing local `#prazdnik` PartyForm anchor; do not recreate the source Tilda popup. | Source `#popup:zal_magnitogorskaya` is a booking-lead popup, while `/magnitogorskaya1/` already renders an equivalent functioning local PartyForm immediately before the hall artboard. The target exists, click changes only the local hash, and no network is introduced. This is scoped to `hall.layout === 'magnit-t396'`; generic venue CTAs keep their existing action. |

| 2026-08-11 19:56 | Source-artboard `/new-year/` | Для `hero.composition: "newyear-artboard"` вывести только route-scoped `NewYearArtboard`: white contained 1160 px hero, six source benefit cards, program, scenario rail, T396 hall, eight show cards, additions/masterclasses, compact FAQ/callback/venues и source dark footer. Generic holiday records, shared header, callback, venue strip и footer на этом route не рендерятся; `/new-year-2025/` остаётся redirect на `/new-year/`, SEO страницы не меняется. | R15/source live фиксирует 34 record sequence с высотами 9123/11560 px (1440/390). Точные bitmap для hero, program, five scenarios, hall, all 8 shows and both five-card rails зеркалированы локально; remote Tilda refs не выводятся. Live в момент реализации добавляет динамический review widget и map, но R15 metric masks оба: renderer сознательно сохраняет R15 review heading/divider и не добавляет external map, чтобы не вносить неподконтрольный runtime height. Desktop footer record 310 px отражает source one-pixel overlap с нижним footer record и даёт canonical total 9123. Все popup CTA заменены на local `#prazdnik`; PartyForm hidden до target, поэтому исходный первый кадр не загрязняется generic form. |
| 2026-08-11 19:56 | Lazyweb `/new-year/` | Обязательный Lazyweb report запущен от текущего screenshot source-backed New Year landing и завершился без degraded slots: https://www.lazyweb.com/report/lazyweb/70670552-95ff-4f33-8654-9ec51f86038b/?source=create. | Report открыт через private handoff; его общие suggestions не подменяют raw/live Tilda content и R15 geometry, которые остаются источником истины для parity. |
| 2026-08-11 20:02 | Полный source-artboard `/prazdnik-maxi/` | Включить только data-gated `hero.composition: "maxi-artboard"` и отдельный `MaxiArtboard`; generic Holiday hero/sections/breadcrumb/callback/venues/footer на этой route не рендерить. Перенести 30 source record boundaries, dark repeating artboard, hero, price, feature/timeline rails, 6 квестов, 8 шоу, 13 package cards, local three-photo hall, video/gallery rails, local review snapshot, callback, venue list/map placeholder и footer. | R15 был 6.39%/5.57% при generic clone, тогда как raw `_capture/pages/prazdnik-maxi.json` задаёт независимую последовательность `rec2262811501…rec2262811951`: 11056/13463 px (1440/390). Для четырёх дробно-округлённых Tilda boundaries используются разницы соседних raw `t`, поэтому canonical starts `quests=2596/3107`, `package=4633/6174`, `hall=6346/7299`, `callback=9257/10491` и document end совпадают с capture. 26 exact rendered source bitmaps зеркалированы локально; shared Holiday routes остаются без новых стилей/markup. |
| 2026-08-11 20:02 | Действия popup/slider `/prazdnik-maxi/` | Все source Tilda/Marquiz popup CTA заменить на local `#callback` с существующим `data-lead-form`; hall retains a local 3-slide next/prev rail rather than decorative `1 из 3`. Не добавлять remote assets или map/review widgets. | Source `#popup:*` не является локальным URL. Local callback preserves a real conversion action with no third-party popup request; Playwright verifies `1 из 3 → 2 из 3` and image replacement on both viewports. Built source contains only `/assets/...` image paths and no `https://static.tildacdn.com`; controlled map/review substitutes remain deterministic. |
| 2026-08-11 20:02 | Lazyweb `/prazdnik-maxi/` | Не выдумывать report URL и не делать третий запрос: after successful local screenshot upload/resolve, `lazyweb_generate_report` дважды завершился `HTTP 400 image_url_fetch_failed`. Продолжить по raw/live Tilda DOM и R15 evidence. | Provider-side image fetch failed after the signed-image flow; report не был создан. Обязательная попытка и один retry выполнены, но source parity опирается на доступные measured records/assets rather than absent design suggestions. |
| 2026-08-11 20:13 | Полный source-artboard `/amongus-land/` | Включить только data-gated `hero.composition: "amongus-artboard"` и отдельный `AmongUsArtboard`; generic Holiday hero/sections/breadcrumb/venue/footer на этой route не рендерить. Перенести raw R15 sequence `rec1049626221…rec1036403686`: hero, intro, video, Standard → Super → Maxi packages, четыре шоу, четыре additions, trust mosaic, eight-photo gallery, bonus, venue pills и dark source footer. Все source popup/video/order CTAs заменить на local `#prazdnik`; local `PartyForm` скрыта до `:target`. | R15 — 2.11%/2.19% при generic clone и 17/17 source records против 13/13 clone records. Raw/live Tilda and full R15 pairs фиксируют canonical document heights 8106/13265 px (1440/390), включая exact one-pixel mobile gap after additions; `reviews` и `map` остаются existing bounded metric masks. 33 rendered hero/card/gallery/bonus bitmaps зеркалированы locally under `/assets/static.tildacdn.com/`; no remote Tilda/Marquiz URL is rendered. Existing SEO, route links and functional local lead form are retained. |
| 2026-08-11 20:13 | Lazyweb `/amongus-land/` | Не выдумывать Lazyweb report: `lazyweb_request_image_upload` успешно вернул signed target, но direct upload current source screenshot завершился `HTTP 400`; report generation therefore was not started and URL отсутствует. | Это provider upload failure after a healthy Lazyweb status check; повторная/фиктивная генерация не давала бы проверяемого evidence. Raw/live Tilda DOM and R15 screenshot geometry remain authoritative for this parity implementation. |

| 2026-08-11 20:13 | Local controls for the Vypusk T923/T396 sliders | Keep the source first-frame artboard geometry, but expose every locally mirrored scenario, addition and hall photo through route-only arrows, dot tabs, keyboard arrows/Home/End and touch swipe. The T923 rows wrap manually; the T396 hall uses its captured three-second loop. | Raw `rec888951784` and `rec888951793` declare four-per-row T923 rails with arrows, bullets and `data-slider-with-cycle="true"`; `rec888951802` declares 8px `#474747/#ff6900` dots and `autoplay=3`. The implementation is isolated to `VypusknojKalmarArtboard` and a local script, preserves the initial desktop `-280px` and mobile `0px` card offsets, honours `prefers-reduced-motion` used by controlled capture, and performs no remote request. |
| 2026-08-11 20:13 | Stale FAQPage schema on `/vypusknoj-kalmar/` | Suppress FAQ JSON-LD only when `hero.composition === 'vypusknoj-artboard'`; retain the generic FAQ rendering/schema path for every other holiday route. | The Vypusk special composition intentionally excludes generic sections, so its legacy FAQ data was not visible. Built clone nevertheless emitted FAQPage/`Сколько стоит выпускной?`, while the raw source document has neither visible FAQ nor FAQ JSON-LD. The data-level route predicate is minimal and does not alter generic SEO behavior. |

| 2026-08-11 20:27 | Полный source-artboard `/minecraft-lend/` | Включить только при `hero.composition: "minecraft-artboard"` отдельный `MinecraftArtboard`; generic Holiday hero/sections/breadcrumb/callback/venues/footer на этой route не рендерить. Перенести R15/R11 sequence `rec1760074751…rec817913403`: source hero, intro/photo/character, three package bitmap, eight show cards, additions rail, trust mosaic, gallery, gift CTA, venue chips и dark footer. Все captured bitmap остаются local `/assets/static.tildacdn.com/...`; source popup CTA заменены на local hidden-until-target `PartyForm#prazdnik`. | R15 baseline был 6.89%/9.11% при generic clone и canonical source heights `7116/11290` (1440/390), а source metric masks охватывают только runtime map/reviews. Raw visual-detail округляет три mobile record heights несовместимо с соседними starts: для exact endpoints приняты deltas `additions=660`, `venues=568`, `copyright=359`, а не displayed `661/567/360`; все остальные raw boundaries сохранены. SEO title/description/H1 оставлены byte-exact; isolated gate не меняет другие Holiday routes, а built page не выводит remote Tilda/Marquiz URL. |
| 2026-08-11 20:27 | Lazyweb `/minecraft-lend/` | После successful health, signed screenshot upload и resolve выполнить ровно один retry `lazyweb_generate_report`; после повторяемого `HTTP 400 image_url_fetch_failed` прекратить попытки и не выдумывать report URL. | Provider returned the same fetch failure twice despite the required presigned-image flow. Raw/live Tilda records, source screenshots and measured R15 geometry therefore remain the implementation authority; no third request was made. |
| 2026-08-11 20:36 | Полный source-artboard `/roblox-land/` | Включить только при `hero.composition: "roblox-artboard"` отдельный `RobloxArtboard`; generic Holiday hero/sections/breadcrumb/callback/venue/footer на этой route не рендерить. Перенести raw/R15 sequence `rec884420602…rec884323138`: hero, intro, video, Standard → Super → Maxi, четыре show cards, четыре additions, первый visible hall, trust mosaic, bonus, девять venue links и source footer. Все Tilda/Marquiz/video CTA заменить на local hidden-until-target `PartyForm#prazdnik`. | R15 baseline 44.52%/31.65% был generic page при source heights 8169/13711 px (1440/390). Raw/live and the full original pairs fix 17 visual records; `map`/`reviews` remain existing metric masks, so implementation intentionally does not create a 475 px map gap. Mobile raw record rounding adds 1 px after venues and footer-bottom is 1 px larger than document end; data preserves source records with `venuesGap=1` and `documentTrim=-1`. Hero foreground, video art, packages, cards, hall, trust and bonus bitmaps are mirrored under local `/assets/static.tildacdn.com/...`; 9 venue actions stay local route links, SEO remains untouched and no remote Tilda/Marquiz reference is emitted. |
| 2026-08-11 20:36 | Lazyweb `/roblox-land/` | Не выдумывать report URL и не выполнять третий запрос: after healthy service, signed upload and resolve of the current R15 source screenshot, `lazyweb_generate_report` twice returned `HTTP 400 image_url_fetch_failed`. Continue from raw/live Tilda and R15 evidence. | Provider-side fetch failure happened after the prescribed upload path completed successfully. The required attempt plus one retry occurred; no report URL exists, and raw measured records/assets remain the parity authority. |

| 2026-08-11 20:49 | Полный source-artboard `/den-rozhdeniya-na-vr-arene/` | Включать `VrBirthdayArtboard` только при `hero.composition: "vr-birthday-artboard"`; не рендерить generic Holiday hero/sections/breadcrumb/callback/venues/footer на этой route. Перенести 23 source records: blank header, hero, games, packages, shows, hall, video, certificate, gallery, callback, venue chips и dark footer; exact local Tilda bitmap используются в responsive artboard, а source popup/Marquiz CTA заменены working local `#callback` form. | R11/R15 original geometry фиксирует source canvas 6 850 px desktop и 9 048 px mobile; anchors games/packages/hall/callback равны `694/1378/2986/5506` и `768/1542/3765/6553`. Raw document также содержит reviews/map, но canonical visual comparator имеет `metricMasks:["map","reviews"]`, поэтому эти external widgets намеренно не создают искусственный gap. Для двух mobile records display-rounded height расходится на 1 px с соседним record start; authoritative endpoints сохранены (shows 468 px, venues 538 px). SEO byte-exact сохранено, built DOM и request observation не содержат remote Tilda/Marquiz URLs. |
| 2026-08-11 20:49 | Lazyweb `/den-rozhdeniya-na-vr-arene/` | Использовать completed report [0ea5eca3-3cfc-44df-a6ba-04e66edfce82](https://www.lazyweb.com/report/lazyweb/0ea5eca3-3cfc-44df-a6ba-04e66edfce82/?source=create) как independent UI review; source raw/live records остаются visual authority. | Presigned screenshot upload и resolve завершились, report generated without degradation/failure and was opened. Это не заменяет controlled parity capture и не создаёт source request. |

| 2026-08-11 22:07 | Полный source-artboard /igra-v-kalmara-lend/ | Вместо generic Holiday sections включать только при hero.composition: kalmar-landing-artboard отдельный KalmarLandingArtboard: 20 R23 records, white/wave intro/features, три source package PNG, четыре show cards, local T923 additions rail, two-record T396 hall pager, trust mosaic, gift, venue links и footer. Generic breadcrumb/callback/venues/footer не рендерить; Tilda popup заменить local hidden-until-target PartyForm#prazdnik. | R23 фиксирует genuine generic residual 2.41%/1.99% и page-height delta −18.11%/−11.59%, с source endpoints 7 499/11 054 px. Raw/live rec866999798…rec866472902 фиксирует exact ordered records, а rec871009912/#915 — две площадки behind arrows. Все art paths локальны; первый некорректно реконструированный набор additions/trust URLs вернул Tilda 504 и был заменён exact paths из live record HTML, а не повторно скачан. CSS route-scoped: другие Holiday routes не получают white/wave styling или rail geometry. |

| 2026-08-11 23:42 | `/amongus-land/` R27 source layers and exact record map | Keep the route-specific `AmongUsArtboard` and add the raw `rec1049626221…rec1036403686` identity map to its visible source boundaries. The R27 desktop source inspector legitimately omits `rec1036403626`, so the desktop venue pills remain rendered as an overflow child of `rec1036403621` and reserve their 164px flow space before `rec1043705606`; the mobile-only visible venue section owns `rec1036403626`. This preserves the visual grid rather than masking or removing it, producing the factual 16 desktop / 17 mobile capture boundaries. Reconstruct the trust block from all eight raw T396 layers (300×285 at desktop offsets 37/322; 233×223 from mobile offset 72), including the source mobile reorder safety before cartoon. Split the footer into `rec1036403681` navigation/legal content and `rec1036403686` logo/contact/copyright with source address copy and local actions. | R27 reports height delta 0/0 but px 14.84/13.815, missing `rec1036403686`, and clone gallery/footer extras. Raw/live `rec1036403561` proves an 8-tile source mosaic while the clone had six flat cards and two empty tiles; it also supplies the eight exact Tilda bitmap paths mirrored locally. Raw `rec1036403681/3686` supplies the missing `УЛИЦА 40-ЛЕТИЯ ПОБЕДЫ, 216`, split footer copy, logo and contact semantics. Lazyweb health, upload and resolve succeeded, but the one required report generation returned provider `image_url_fetch_failed:400`; no report URL is claimed. No comparator, generic Holiday route, build, capture or commit changed. |

| 2026-08-11 23:46 | `/prazdnik-maxi/` R27 controlled record map and source-absent blocks | Replace the legacy 30-record Maxi stack with the 28 records actually visible in R27: map every visible boundary `rec2262811501` through `rec2262811951`, including the formerly unmapped video rail `rec2262811681`; remove only the clone-only 865 px local review-card widget and 385 px gray map placeholder; remove the 130 px desktop show-grid margin. Keep the measured review heading, gallery, every remaining local accessible rail and the local callback form. | R27 records `28/28 source` versus `30/30 clone`, documents `9808/12216` source heights against `11186/13463`, and flags the missing video record plus clone video/review/gallery extras. Its controlled original goes directly from `rec2291027701` review heading to `rec2291039451` gallery heading and has no map record; raw confirms the retained video/gallery bitmap rails and live widget/map behaviour explains the older raw-only 30-record model. `mobileFooterSeam: 1` preserves the actual one-pixel gap between the 853 px footer record and copyright start without falsifying a record height. The branch stays route-only in `MaxiArtboard`; no mask or comparator threshold changes. |

| 2026-08-11 23:46 | Lazyweb `/prazdnik-maxi/` R27 attempt | Health, presigned JPEG upload and resolve were attempted against the current R27 desktop source screenshot. Resolve returned the provider quota hard-stop, so do not generate, retry, poll or invent a report URL. | Lazyweb returned: “You've used this month's free Lazyweb tool allowance.” Its instruction explicitly says not to retry, poll or fall back. Raw/live Tilda plus R27 remain the source-parity authority for this bounded correction. |

| 2026-08-11 23:59 | `/vypusknoj-kalmar/` R27 hall and footer boundaries | Keep the route-only `VypusknojKalmarArtboard`, but replace its merged review/footer boundaries with all 23 R27 `rec…` records: three separately masked review continuations (`315/255/73` desktop; `210/150/73` mobile), `rec888951904` footer navigation (`341/856`) and `rec888951907` contact/logo continuation (`194/360`). Render the source hall’s rounded equipment card, five source equipment bullets, local source pager SVGs, exact footer venue order/copy, hours and email. Preserve the normalized local phone prop instead of copying the live call-tracking number. A `-1px` desktop footer flow trim preserves the measured 8,410px document while the explicit `rec888951907` marker retains its R27 194px boundary. | R27 is height-exact but reports `23/20` source/clone records, missing `rec888951907`, an extra merged `организация мероприятий` block, and missing hall equipment copy. Raw `rec888951802` shows the source T396 overlay/card; raw `rec888951904/907` supplies the nine footer venue labels, footer split, local PNG logo, hours and `marketing.chezaquest@yandex.ru`. The footer phone is normalized by the comparator as `<phone>` and changes in live capture, so only the existing local normalized phone action stays dynamic. No mask, threshold, comparator or generic Holiday layout changed. Lazyweb was not retried: the immediately preceding provider quota hard-stop explicitly forbids generation/retry/poll/fallback, so no report URL is fabricated. |

| 2026-08-12 00:23 UTC | `/den-rozhdeniya-uznik-azkabana/` R27 source visual layers | При точных 33 границах 9184/12332 заменить только в `AzkabanArtboard` generic black/yellow card layer на extracted source layers: framed hero, native package bitmap cards, 4×2/scroll show rail, hall image+list, review badges, icon features, one-frame gallery, dark 8-tile trust mosaic, 360px source alternative rail, yellow booking art with the existing functional local form, exact 9 venue links and footer copy. Store every newly used source image locally under `public/assets`; retain record-height CSS variables, both `data-lead-form` forms and generic HolidayPage unchanged. | R27 has zero height delta but `44.915/39.35 px`; raw `_capture/pages/den-rozhdeniya-uznik-azkabana.json` and R27 pairs identify rendering, not comparator/mask fault. The prior artboard cropped the source package bitmaps, emitted generic kickers/number cards/yellow trust columns and dark booking card. No masks/threshold/comparator changed. Lazyweb was not retried: preceding provider quota hard-stop explicitly prohibits retry/poll/fallback, so no report URL is fabricated. |

## Verification

- 2026-08-12 00:23 UTC: `/den-rozhdeniya-uznik-azkabana/` RED `node --test tests/azkaban-artboard-parity.test.mjs` failed before `sourceParity.visual` existed. GREEN after the route-only data/component correction: the same focused suite passed 3/3, including local source visual assets and route-only layer contracts. Route-local static resolver `node scripts/asset-audit.mjs` passed (`1693 public assets, 1237 referenced, 173 source-only lazy/runtime, 8 build-only, 456 report-only unreferenced`); direct `@astrojs/compiler-rs` parse and transform reported zero diagnostics; `git diff --check` is clean. Per bounded instruction, npm build, production DOM smoke, parity capture, comparator edits and commit were not run; parent integration CI/capture remains the compile and visual gate.

- 2026-08-11 23:59 UTC: `/vypusknoj-kalmar/` RED `node --test tests/vypusknoj-kalmar-artboard-parity.test.mjs` failed against the old 20-record merged review/footer model. GREEN after the route-only data/component correction: the focused suite passed 5/5, including all 23 `data-parity-record` boundaries, hall equipment copy, footer source copy and local carousel controls. A route-local static resolver found all 35 `sourceParity` `/assets/` paths under `public`; `git diff --check` is clean. Per bounded instruction, npm build, production DOM smoke, parity capture, comparator edits and commit were not run; parent integration build/capture remains the compile and visual gate.

- 2026-08-11 23:42 UTC: `/amongus-land/` RED focused contract failed before `recordIds`, the eight-layer trust model and locally mirrored raw bitmaps existed. GREEN `node --test tests/amongus-artboard-parity.test.mjs` passed 4/4; `python3 _capture/check_assets.py` reported `битых путей нет`; `git diff --check` was clean. A direct Astro compiler parse could not run because `@astrojs/compiler` is absent from this dependency tree; per bounded instruction, npm build, current-dist smoke, parity capture and commit were not run. Parent integration build remains the compile gate.

- 2026-08-11 23:46 UTC: `/prazdnik-maxi/` RED after updating the R27 contract: `node --test tests/prazdnik-maxi-artboard-parity.test.mjs tests/maxi-artboard-rails.test.mjs` failed on the old 30-record/review rail model. GREEN after the route-only data/component correction: the same focused suite passed 6/6, resolving every local Tilda bitmap and retaining keyboard/native-scroll rails for features, timeline, shows, package, video and gallery. Static record totals are `9808/12216`; `git diff --check` is clean. Per bounded instruction, npm build, current-dist smoke, parity capture, comparator edits and commit were not run; parent integration build/capture remains the compile and visual gate.

- 2026-08-11 22:07 UTC: /igra-v-kalmara-lend/ RED tests/kalmar-programs-artboard-parity.test.mjs сначала подтвердил отсутствие source branch/component/data. GREEN focused suite — 4 tests, 0 failures. npm run build собрал 68 pages; npm run verify:assets (0 issues) и npm run verify:production прошли. DOM-only Chrome smoke current dist на 1440/390 подтвердил exact source height 7499/11054, 4 local addition slides, 2 local hall slides, hidden local #prazdnik form, успешный CTA reveal, zero remote requests, runtime failures и missing artboard images. Full parity capture и commit intentionally not performed. Lazyweb upload/resolve succeeded; report job f4e6db1c-8a8c-46cc-b6e0-d86e9d41c303 was still pending at source-freeze handoff, so no URL is claimed.

- 2026-08-11 11:29 UTC: `npm run build` без `SITE_BASE` успешно; `grep -c chezakvest-preview dist/index.html` вернул `0`.
- 2026-08-11 11:30 UTC: исходный `npm test` успешно завершён: 71 тест, 0 ошибок.
- 2026-08-11 15:14 UTC: после source fixes `npm test` завершён: 106 тестов, 0 ошибок; `npm run build` без `SITE_BASE` и `npm run verify:production` успешно завершены. Найденные три `#prazdnik` anchor были исправлены в source до R11.
- 2026-08-11 15:59 UTC: focused `node --test tests/home-slider.test.mjs` завершён: 8 тестов, 0 ошибок. Build/capture намеренно оставлены следующему R12 verification loop.
- 2026-08-11 17:52 UTC: focused `node --test tests/quest-venue-card-data.test.mjs tests/quest-venue-layout-parity.test.mjs tests/wave4-parity.test.mjs` завершён: 19 тестов, 0 ошибок.
- 2026-08-11 17:53 UTC: `npm run build` успешно собрал 68 страниц; built `/magnitogorskaya1/` содержит ровно 14 `.card`, а built `/ono/` содержит captured `/assets/q/7eba8c62f8.webp`. `git diff --check` завершён без вывода.
- 2026-08-11 18:34 UTC: `node --test tests/magnit-hall-artboard-parity.test.mjs tests/quest-venue-layout-parity.test.mjs tests/quest-venue-card-data.test.mjs tests/wave4-parity.test.mjs` — 22 tests, 0 failures; `npm run build` — 68 pages; `git diff --check` — clean. Capture и commit намеренно не выполнялись в этом scoped fix.
- 2026-08-11 18:40 UTC: после финальной коррекции font scaling `node --test tests/magnit-hall-artboard-parity.test.mjs` — 2 tests, 0 failures; `npm run build` — 68 pages; `git diff --check` — clean. DOM geometry check без screenshot подтвердил desktop photo/panel `200/600/400` и `701/540/341`, а mobile photo/panel `-24/439/275` и `12/366/318`.
- 2026-08-11 18:25 UTC: focused `node --test tests/ono-source-blocks-parity.test.mjs tests/quest-venue-layout-parity.test.mjs tests/quest-venue-card-data.test.mjs` завершён: 17 тестов, 0 ошибок. `npm run build` успешно собрал 68 страниц; в built `/ono/` есть только route-scoped `qvideo--source-t347` и `cards--source-t121`, в `/zvonok/` остаются plain generic classes. `git diff --check` завершён без вывода.
- 2026-08-11 18:41 UTC: focused `node --test tests/podkluch-artboard-parity.test.mjs tests/holiday-r10-parity.test.mjs tests/campaign-hero-layers.test.mjs` завершён: 9 тестов, 0 ошибок; `npm run build` успешно собрал 68 страниц; `npm run verify:assets`, built-artboard contract и `git diff --check` прошли. Capture и commit намеренно не выполнялись в scoped fix.
- 2026-08-11 18:46 UTC: read-only сверка финальных R17 Pod Kluch artifacts: `visual-matrix-podkluch.csv` и `round-17-podkluch/visual-detail.json` совпадают по `px_390=19.84`; ранее сообщённое `26.19` было transient intermediate значением и отозвано. Новая съёмка, source edit, build и commit не выполнялись.
- 2026-08-11 19:01 UTC: T396 `/ono/`: сначала новый `tests/ono-venue-hall-artboard-parity.test.mjs` ожидаемо упал до реализации; затем focused `node --test tests/ono-venue-hall-artboard-parity.test.mjs tests/ono-source-blocks-parity.test.mjs tests/quest-venue-layout-parity.test.mjs tests/quest-venue-card-data.test.mjs` — 20 tests, 0 failures; `npm run build` — 68 pages. DOM-only check готового `dist` (без screenshot) подтвердил section `735/990`, 1440 rail/panel `80,8,600,400` / `581,138,540,341` и 390 `-24.375,415.672,439,275` / `12.188,148.688,365.688,353.5`; generic `.qvenue` отсутствует, `/40letpobedy216/` и `tel:` links сохранены. `git diff --check` — clean. Capture и commit намеренно не выполнялись.
- 2026-08-11 19:15 UTC: T131 `/ono/` slider test сначала ожидаемо упал без `celebrationVenues`/controls; затем focused suite (`ono-venue-hall-artboard`, `ono-source-blocks`, quest venue data/layout) — 21 tests, 0 failures; `npm run build` — 68 pages. DOM-only Playwright check against current `dist` on `8799`, no screenshot: at both 1440 and 390 exactly one slide is visible; next changes `1 из 2` / `Улица 40-Летия Победы, 216` / `2 ЗАЛА…` to `2 из 2` / `Улица Магнитогорская, 1` / `3 ЗАЛА…`, with `/magnitogorskaya1/` link; prev restores slide 1. Both slides retain `735/990` section height and source artboard rail/panel/CTA geometry. Capture and commit intentionally not performed.
- 2026-08-11 19:06 UTC: T017/T774 Pod Kluch сначала `node --test tests/podkluch-services-parity.test.mjs` ожидаемо упал (нет data/component); после реализации focused `node --test tests/podkluch-services-parity.test.mjs tests/podkluch-artboard-parity.test.mjs` — 4 tests, 0 failures. `npm run build` собрал 68 страниц, `npm run verify:assets` прошёл (1435 public assets, 945 referenced), built route содержит 8 local Tilda paths в source-порядке, 8 CTA и без remote `static.tildacdn.com` URL; `git diff --check` чист. Capture и commit намеренно не выполнялись.
- 2026-08-11 19:26 UTC: T395/T396 Pod Kluch сначала `node --test tests/podkluch-game-grid-parity.test.mjs` ожидаемо упал без data/component; после реализации focused `node --test tests/podkluch-game-grid-parity.test.mjs tests/podkluch-artboard-parity.test.mjs tests/podkluch-services-parity.test.mjs` — 7 tests, 0 failures. `npm run build` собрал 68 страниц; `npm run verify:assets` (1468 public/1004 referenced) и `npm run verify:production` прошли. DOM-only Playwright check без screenshot подтвердил source geometry: 1440 global `x=142`, artboard `y=122`, poster `360×264`; 390 global `x=35`, artboard `y=82`, poster `320×234`. Desktop tab и mobile select переключают только requested group, local CTA ведёт на `/kvest_v_realnosti_harry_potter_i_krestrazh/`. Capture и commit намеренно не выполнялись.
- 2026-08-11 19:33 UTC: `/vypusknoj-kalmar/` сначала `node --test tests/vypusknoj-kalmar-artboard-parity.test.mjs` ожидаемо упал (нет opt-in composition, component и mirrored assets); после реализации focused `node --test tests/vypusknoj-kalmar-artboard-parity.test.mjs tests/campaign-hero-layers.test.mjs tests/holiday-r10-parity.test.mjs` — 10 tests, 0 failures. `npm run build` собрал 68 страниц; `npm run verify:assets` прошёл (1468 public / 1009 referenced). Built-artboard contract подтвердил source blocks, local `#prazdnik`, пять mirrored asset files в `dist` и отсутствие remote `https://static.tildacdn.com`. `git diff --check` чист. Capture и commit намеренно не выполнялись.
- 2026-08-11 19:36 UTC: `/igra-v-kalmara-lend/` новый `tests/kalmar-programs-artboard-parity.test.mjs` сначала ожидаемо упал (нет `kalmar-programs`, component и local assets); после реализации focused `node --test tests/kalmar-programs-artboard-parity.test.mjs tests/holiday-r10-parity.test.mjs tests/campaign-hero-layers.test.mjs` — 10 tests, 0 failures. `npm run build` собрал 68 страниц; `npm run verify:assets` прошёл (1468 public / 1009 referenced); `git diff --check` чист. DOM-only Playwright check against current `dist` on `8799`, без screenshot, подтвердил exact source section/card/CTA/separator coordinates на 1440 и 390, four `#prazdnik` links, existing target form и zero remote Tilda URLs; click CTA меняет hash на `#prazdnik`. Capture и commit намеренно не выполнялись.
- 2026-08-11 19:56 UTC: `/new-year/` новый `tests/newyear-artboard-parity.test.mjs` сначала ожидаемо упал (нет opt-in composition, component и local source assets); после реализации focused `node --test tests/newyear-artboard-parity.test.mjs tests/campaign-hero-layers.test.mjs tests/holiday-r10-parity.test.mjs` — 10 tests, 0 failures. `npm run build` собрал 68 страниц; `npm run verify:assets` прошёл (1513 public / 1053 referenced), `npm run verify:redirects` подтвердил 103 current legacy records. Built contract подтвердил local artboard/benefit/show/booking markers и zero remote Tilda URLs; `/new-year-2025/` содержит route target `/new-year/`. Capture и commit намеренно не выполнялись.
- 2026-08-11 19:55 UTC: T396 hall galleries: RED `tests/t396-hall-gallery.test.mjs` failed before `src/scripts/t396-gallery.js` existed; CTA regression test failed before the Magnit anchor change. Green focused suite (`t396-hall-gallery`, Magnit/Ono artboard, quest venue layout) — 19 tests, 0 failures; `npm run build` — 68 pages; `npm run verify:assets` and `npm run verify:production` passed. DOM-only Google Chrome checks on current `dist` at 1440 and 390 verified six dot-reachable local slides, wrap from last to first, Ono's second hall, Magnit `#prazdnik` click, 3s autoplay, deterministic reduced-motion first frame, and zero external requests. No capture or commit was performed.
- 2026-08-11 20:02 UTC: `/prazdnik-maxi/` RED `tests/prazdnik-maxi-artboard-parity.test.mjs` initially failed because no `maxi-artboard`, local component or mirrored assets existed; GREEN focused test then passed (3 tests). `npm run build` completed 68 pages; `npm run verify:assets` passed (1513 public / 1053 referenced); `git diff --check` was clean. DOM-only Google Chrome verification against current `dist` at 1440/390, without screenshots, matched source total height `11056/13463` and exact hero/price/quests/package/hall/callback record starts and heights, verified all artboard images local, zero remote Tilda URLs, one working local form, `#callback` CTA, and hall `1 из 3 → 2 из 3`; request observation recorded zero remote network requests. Capture and commit intentionally not performed.
- 2026-08-11 20:13 UTC: Vypusk controls RED `tests/vypusknoj-kalmar-artboard-parity.test.mjs` first failed because `vypusknoj-artboard-controls.js` did not exist; GREEN route test then passed (4 tests). `npm run build` assembled 68 pages, `npm run verify:assets` passed (1542 public / 1083 referenced), and `git diff --check` was clean. DOM-only Google Chrome checks against current `dist` at 1440 and 390 verified all 5/4 cards through dot, arrow, keyboard and mobile touch navigation; hall dot/pager loop, normal 3-second autoplay and reduced-motion deterministic first frame; FAQPage/legacy FAQ copy absent; and zero external requests. Full parity capture and commit intentionally were not performed. A broader shared `holiday-r10` check is currently red only because a concurrent Maxi guard changed its literal expected CallbackForm expression; no test or broad layout was changed to mask it. `verify:production` is currently red only for concurrent `/prazdnik-maxi/` visible-label findings.
- 2026-08-11 20:14 UTC: `/amongus-land/` RED `tests/amongus-artboard-parity.test.mjs` initially failed before the opt-in composition/component/assets existed; GREEN `node --test tests/amongus-artboard-parity.test.mjs tests/campaign-hero-layers.test.mjs` then completed 5 tests, 0 failures. `npm run build` assembled 68 pages; `npm run verify:assets` passed (1542 public / 1083 referenced), and `npm run verify:redirects` confirmed 103 records. Built-route contract verified all source section markers, working local `#prazdnik`/lead form, 34 locally emitted Tilda asset paths present in `dist`, and zero remote static/thb/optim Tilda URL. `git diff --check` was clean. The broader `holiday-r10-parity` literal CallbackForm assertion remains a known concurrent Maxi-guard test drift and was left to the central fix; no capture or commit was performed.
- 2026-08-11 20:27 UTC: `/minecraft-lend/` RED `tests/minecraft-artboard-parity.test.mjs` initially failed because no route opt-in/component/local source asset set existed; GREEN focused `node --test tests/minecraft-artboard-parity.test.mjs tests/campaign-hero-layers.test.mjs tests/holiday-r10-parity.test.mjs` completed 10 tests, 0 failures. `npm run build` assembled 68 pages; `npm run verify:assets` passed (1568 public / 1109 referenced); `git diff --check` was clean. DOM-only Google Chrome verification against current `dist` at 1440/390, without screenshots, matched source total `7116/11290` and records hero `90/550,90/530`, intro `640/690,620/844`, programs `1470/744,1604/1330`, shows `2244/1007,2964/3464`, trust `4051/850,7328/800`, gift `5494/700,8811/500`, footer `6645/277,10154/777`. SEO title/description/H1 are exact; all built images and observed requests are local, and hero CTA changes to `#prazdnik`, reveals one local `data-lead-form`. Full parity capture and commit intentionally were not performed.
- 2026-08-11 20:36 UTC: `/roblox-land/` RED `tests/roblox-artboard-parity.test.mjs` initially failed because `RobloxArtboard.astro` did not exist; GREEN focused `node --test tests/roblox-artboard-parity.test.mjs tests/campaign-hero-layers.test.mjs` completed 5 tests, 0 failures. `npm run build` assembled 68 pages; `npm run verify:assets` passed (1605 public / 1138 referenced); built-route contract found all 19 measured records, 39 local Tilda asset paths and zero remote static/thb/optim Tilda URL. `npm run verify:redirect-targets` passed 103 current URL records and `git diff --check` was clean. No capture or commit was performed.
- 2026-08-11 20:49 UTC: `/den-rozhdeniya-na-vr-arene/` RED `node --test tests/vr-birthday-artboard-parity.test.mjs` first failed before the opt-in composition/component/local assets existed; GREEN focused `node --test tests/vr-birthday-artboard-parity.test.mjs tests/holiday-r10-parity.test.mjs tests/campaign-hero-layers.test.mjs` completed 10 tests, 0 failures. `npm run verify:assets` passed (1616 public / 1157 referenced) and `git diff --check` was clean. DOM-only current-`dist` verification without screenshots confirmed document/record anchors exactly: 1440 `6850`, games/packages/hall/callback `694/1378/2986/5506`; 390 `9048`, `768/1542/3765/6553`; it also found one local callback form and zero remote requests at both widths. Full capture and commit intentionally were not performed.

- 2026-08-11 21:02 UTC: Для четырёх route-scoped source artboards (`/new-year/`, `/minecraft-lend/`, `/amongus-land/`, `/roblox-land/`) выбрать один shared `source-artboard-rail` controller вместо отдельных слайдеров. Он сохраняет captured first frame (`scrollLeft=0`), показывает controls только когда local-card rail реально переполняется, использует native horizontal touch scroll/snap, стрелки, dot tabs с `aria-controls`, live status и `Arrow`/`Home`/`End` keyboard wraparound. `prefers-reduced-motion` отключает smooth movement; autoplay намеренно не добавлен. | Это исправляет only clipped mapped local cards, не меняя source record heights, generic Holiday branches или external URLs. Reused the completed New Year Lazyweb UI review ([70670552-95ff-4f33-8654-9ec51f86038b](https://www.lazyweb.com/report/lazyweb/70670552-95ff-4f33-8654-9ec51f86038b/?source=create)); previously recorded Minecraft/AmongUs/Roblox provider failures remain authoritative and no new report URL was fabricated.
- 2026-08-11 21:02 UTC: New Year mobile show rail больше не скрывает `.newyear-artboard__show-card:nth-child(n+5)`. Все 8 source cards остаются в DOM/scroll rail, while desktop retains the full two-row source frame and suppresses redundant controls when it fits. | Hiding half of a captured local collection made content unreachable; preserving the first card and putting remaining cards behind the same reachable rail matches the source carousel intent without increasing the measured record.
- 2026-08-11 21:02 UTC: Rail repair RED→GREEN: new `tests/source-artboard-rail.test.mjs` initially failed because the shared controller/component did not exist; focused 16-test suite passed after implementation. `npm run build` completed 68 pages; `npm run verify:assets` passed (1616 public / 1157 referenced); `git diff --check` clean. Browser DOM-only smoke at 390 confirmed all 8 rails have all source slides/dots, visible controls and `next` scrolls from first to second; desktop 1440 confirmed `scrollLeft=0` first frame, controls hidden whenever every card fits, and keyboard ArrowRight selects/moves to slide 2. No full capture or commit was performed in this scoped repair.

| 2026-08-11 21:09 | Reachability локальных рядов `/prazdnik-maxi/` | Сохранить measured heights и initial crop всех 30 Maxi records; семь уже перенесённых local collections (features, timeline, shows, package, video, reviews, gallery) сделать route-scoped native horizontal rails с touch scroll, focusable viewport, стрелками, live счётчиком и `Arrow`/`Home`/`End` navigation. Controls не занимают layout и проявляются только при hover/focus; current 3-photo hall rail и callback form не менялись. | Raw/live Tilda artboard остаётся visual authority. Existing Maxi Lazyweb attempt already failed twice with `HTTP 400 image_url_fetch_failed` (decision 20:02), поэтому новый report не запрашивался и URL не выдумывался. CSS overlay сохраняет first frame/record geometry; all content and assets remain local, without remote requests. |
- 2026-08-11 21:09 UTC: Maxi rails RED `tests/maxi-artboard-rails.test.mjs` сначала упал из-за отсутствующего controller; GREEN focused `node --test tests/maxi-artboard-rails.test.mjs tests/prazdnik-maxi-artboard-parity.test.mjs` — 5 tests, 0 failures. `npm run build` собрал 68 страниц; `npm run verify:assets` passed (1616 public / 1157 referenced); `git diff --check` clean. DOM-only 390 smoke clicked each rail to its terminal local item with statuses `8/8`, `6/6`, `8/8`, `13/13`, `3/3`, `3/3`, `5/5`, zero page errors. Full capture/commit intentionally not performed in this scoped repair.

| 2026-08-11 21:15 | Самодостаточный HTML parity-отчёт | Генератор требует одну читаемую пару original/clone для каждого из 1440/390 у каждой строки `visual-matrix.csv`; при отсутствии или нечитабельности снимка прекращает генерацию. В отчёт вставляются только data-URI thumbnails, сохраняется лимит 40 MiB до записи файла, а итоговая сводка указывает точное число встроенных снимков. Для каждого route показаны исходные matrix-диагностики, отдельная подпись «что было не так / что исправлено», exact route-scoped `known-gaps` и live content drift. `known-gaps.csv` и `content-drift.csv` читаются как optional: отсутствие файла рендерит явную пустую таблицу, а не ломает отчёт. | Полная сдача требует 67×4 evidence, посекционных/rule diagnostics, подписи без выдуманных resolved claims и отдельной таблицы «сайт изменился с момента съёма». Caption берёт проблему только из `visual-matrix.csv`; поле `fixed` цитируется лишь если заполнено, иначе прямо сообщает, что подтверждённого route-specific исправления нет. Сознательные замены и drift отображаются как самостоятельные факты из CSV и не интерпретируются как закрытие residual. R15 smoke: 67 routes, 268 data-URI screenshots, 3 drift rows, 3.14 MiB, отсутствующих пар нет. |

| 2026-08-11 21:43 | Comparator: source-artboard record inventory | In `scripts/parity-visual.mjs`, treat top-level `[data-parity-record]` nodes as authoritative ordered capture boundaries; preserve their `rec…` identity for exact source pairing, including blank spacers/media records. For older route-scoped artboards without markers, flatten only direct visible children of a `*-artboard` / `[data-source-artboard]` root inside `<main>`. Exclude generic ancestors that contain a specific record and never recurse into nested cards/slides or nested explicit records. | The previous clone selector inspected only direct `main > section/article`, so custom `<div class="*-artboard">` routes reported zero clone sections and `px=n/a`. This is a comparator diagnostic repair, not a content mask or threshold change: paired crops remain subject to the existing 90/88 pixel and 10/15 height gates. |

- 2026-08-11 21:43 UTC: RED→GREEN `node --test tests/parity-visual.test.mjs` (17 tests) covers explicit records, nested-record/wrapper deduplication, old-artboard direct-child fallback and blank exact `rec…` pairing; `git diff --check` clean.
- 2026-08-11 21:39 UTC: isolated R25 `/den-rozhdeniya-na-vr-arene/` capture after the explicit contract: 23/23 source/clone records, 23 paired in both viewports, numeric pixel medians 64.14/59.29, page height delta 0/0, max section delta 0/0.21, zero clone runtime/external/failed/overflow defects. The route remains `needs_fix` for visible parity residuals, not hidden by this comparator repair.
- 2026-08-11 21:43 UTC: isolated R26 `/amongus-land/` capture after the legacy-artboard fallback: 17/17 source/clone sections and numeric medians 13.835/13.82, with page height delta 0/0 and zero clone runtime/external/failed/overflow defects. It remains `needs_fix`; semantic residuals and low pixels remain explicit diagnostics.

| 2026-08-11 22:03 | `/brawl_stars/` T265/T1033/T774 source body | Добавить data-gated `sourceParity.kind: "brawl-source"` только для `/brawl_stars/`: три T265 feature strips, T1033 «Дополнительная информация», source booking copy, callback перед related record, и полный T774 grid из 13 карточек без generic PartyForm. `QuestPage` сохраняет прежний generic order под `!isBrawlSource`; `CardsRow` получает opt-in `t774` только для poster cards. Desktop grid использует exact 5×220, gaps 25/32 и record height 806; mobile — 2×151×166, gaps 20/21 и height 1337. | R23 source `rec674011566–568`, `rec674011569–570`, `rec674011575`, `rec1144740346`, `rec222205461` фиксирует эту последовательность; raw Tilda positions подтверждают отличающийся visual order T774 на 1440 и 390, поэтому data carries viewport-specific `sourceOrder`. Две обязательные Lazyweb попытки после успешного screenshot upload обе вернули provider `image_url_fetch_failed:400`; report URL не выдумывался, live Tilda/raw capture остаётся visual authority. Никакие comparator, HolidayPage, contacts или source assets не менялись. |

- 2026-08-11 22:03 UTC: RED `node --test tests/brawl-source-parity.test.mjs` сначала упал без Brawl data/style/opt-in; GREEN focused Brawl + Ono/Quest contracts — 13 tests, 0 failures. `npm run build` собрал 68 pages; `npm run verify:production` passed. DOM-only Playwright smoke на fresh `dist`, без screenshot, подтвердил на 1440 и 390: body route guard, 3 strips, 4 information entries, source booking без CTA, callback before 13-card grid, zero PartyForm/arrows/runtime errors, exact computed 5×220 / 2×151 columns и source visual card order. `npm run verify:assets` сейчас red только по четырём unrelated concurrent refs in `igra-v-kalmara-lend`/`vypusknoj-kalmar`; every Brawl hero/related local asset resolves. Capture и commit намеренно не выполнялись.

| 2026-08-11 22:06 | `/den-rozhdeniya-uznik-azkabana/` measured black/yellow artboard | Replace only this holiday route’s generic composition with `AzkabanArtboard`: 33 captured record dimensions (9184 desktop / 12332 mobile), source order from header through local footer, the missing review heading, and two local party lead forms (`#quiz`, `#prazdnik`). The guard is `hero.composition === 'azkaban-artboard'`; generic `HolidayPage` remains unchanged for every other route. | R23 gives `15.18/13.82` px and a −22.22% desktop height delta because the generic page omitted the source yellow/black hierarchy, review boundary, and source-order conversion blocks. Record data is route-owned; all package and shown imagery resolves under local `/assets/`, and the low-resolution gallery image is not used at a desktop size that violates the local raster contract. The required Lazyweb report request was attempted with the R23 desktop source screenshot but the provider timed out after 300 seconds; no report URL was fabricated. |

- 2026-08-11 22:06 UTC: TDD RED `node --test tests/azkaban-artboard-parity.test.mjs` failed before route data/component existed; GREEN focused contract passed 2/2. `npm run build` generated 68 pages and sequential `npm run verify:production` passed. Built-DOM smoke found 26 semantic source record markers, two local `data-lead-form` party forms, `#pakety`/`#prazdnik` targets, and no remote Tilda URL. Fresh-dist Playwright smoke at 1440 and 390 (no screenshot) matched exact document heights `9184/12332`, had no overflow, failed request, console error, external request, or image-density failure. `npm run verify:assets` is currently red only for 12 concurrent missing references emitted by `/igra-v-kalmara-lend/`; all four Azkaban-specific local static assets resolve. Capture and commit intentionally were not performed in this scoped fix.

| 2026-08-11 22:14 | `/kids/` measured white/orange source artboard | Replace only `/kids/` generic holiday composition with data-gated `KidsArtboard`. Route data owns all 43 original Tilda records (desktop 13 846 px; mobile 13 264 px), including the source review heading, invitation form sequence, gallery, venue strip and split footer. `HolidayPage` gets only a `hero.composition === 'kids-artboard'` branch and suppression of its generic siblings. | R27 reports source/clone `43/15`, `px_1440=45.965`, `px_390=39.745`, and a −38.88% desktop height deficit. Raw live `/kids` confirmed source IDs/order: review `rec844797129`, invitation `rec844797133/134`, gallery `rec844797138/139`, venues `rec1100733926/3931`, footer `rec1100733991/3996`. The route component renders those records with local page assets and local lead forms; it does not use a remote Tilda runtime. Required Lazyweb submission with the R27 source screenshot timed out after 300 seconds, so no provider report URL was fabricated. |

- 2026-08-11 22:14 UTC: RED `node --test tests/kids-artboard-parity.test.mjs` failed before the composition/component existed; GREEN focused route contract passed 2/2 after implementation. The test resolves all 39 `/assets/` paths referenced by `kids.json` plus route-specific local assets. `git diff --check` is clean. Per bounded task instruction, npm build, full capture, commit and parity-script edits were not run.

| 2026-08-11 23:18 UTC | `/kvest_v_realnosti_zapad/` R27 source record stack | Replace only this route's generic QuestPage body with `ZapadSourceArtboard`, gated by `sourceParity.kind === "zapad-source"`. The route owns all 14 measured boundaries: hero, breadcrumbs/gap, both visible 18px `Error get alias` records, story, gallery, spacer, orange features/information/price band, booking, callback, venue heading/chips and footer spacer. Generic related cards and PartyForm are absent only in that branch; the shared QuestPage remains in the alternate branch for unmarked routes. | R27 raw/live source establishes `rec223203317` through `rec1100733986`: desktop source 4793px / clone 5713px and mobile 5503px / clone 6657px before the repair, with 42.41/40.36 px residual. Existing local hero, boots and gallery assets are reused; no remote Tilda asset, comparator, HolidayPage or contacts change was made. The completed grounded Lazyweb report is [3d7c1c9c-c275-4092-a383-0bc9a5c12da3](https://www.lazyweb.com/report/lazyweb/3d7c1c9c-c275-4092-a383-0bc9a5c12da3/?source=create) (not degraded). |

- 2026-08-11 23:18 UTC: RED `node --test tests/zapad-source-parity.test.mjs` failed before the route data/component/style existed; GREEN passed 4/4 after implementation. Focused regression suite (`zapad-source-parity`, Brawl, Ono and generic quest layout contracts) passed 21/21. DOM-only Google Chrome smoke on the existing Astro dev server, without screenshot, found no header/related/PartyForm, all 14 source record markers in source order, zero runtime errors and zero external requests at both 1440 and 390. Its exact record coordinates/heights match source at both widths: desktop `0+900`, `900+54`, `954+73`, `1027+18`, `1045+350`, `1395+626`, `2021+140`, `2161+980`, `3141+290`, `3431+480`, `3911+18`, `3929+132`, `4061+191`, `4252+60`; mobile `0+760`, `760+54`, `814+73`, `887+18`, `905+480`, `1385+325`, `1710+50`, `1760+900`, `2660+330`, `2990+630`, `3620+18`, `3638+111`, `3749+537`, `4286+60`. Document height is now 4850/5597, leaving only the shared footer delta versus source 4793/5503 for the next visual round. Per bounded task, npm build, parity capture, comparator edits and commit were not run.

| 2026-08-11 23:28 | `/strashnye-kvesty/` R27 source category artboard | Replace only this category route with `HorrorCategoryArtboard`, gated by `sourceParity.kind === "horror-category-artboard"`. The data-owned stack preserves all 14 Tilda boundaries: header, hero, breadcrumbs, game heading, 13-card source-order grid, 10-image gallery rail, dark certificate, divider, callback, venue heading/chips, spacer and split footer. Generic `CategoryPage` remains the alternate branch for every unmarked category. | R27 records source/clone `14/7`, px `55.85/39.95`, source heights `5416/8892` and clone `5042/8848`. Raw live `/strashnye-kvesty` confirms cards `rec2229792331`, three-card gallery `rec1202221301`, certificate `rec2233066361`, callback `rec2233039911`, venue records `rec2233216901/6911`, and footer `rec2233217031/7041`. The mobile inspector has a one-pixel footer start seam, carried explicitly in route data rather than silently changing record geometry. Every one of the 13 card hrefs remains reachable and resolves to a local source/canonical image; the source certificate PNG and three ornament SVGs are local `/assets/` paths. No remote Tilda runtime or generic category style is rendered in this branch. Grounded Lazyweb report completed without degradation and was opened: [8b03c2a1-67c0-4b97-9d7c-906be541fa64](https://www.lazyweb.com/report/lazyweb/8b03c2a1-67c0-4b97-9d7c-906be541fa64/?source=create). |

- 2026-08-11 23:28 UTC: RED `node --test tests/horror-category-artboard-parity.test.mjs` failed before the route data/component existed; GREEN focused contract passed 2/2 after implementation and resolves all local route assets plus the 13 canonical fallback card assets. `git diff --check` is clean. The optional existing `holiday-r10-parity` regression remains red only on its already-concurrent literal HolidayPage callback guard (`generic holiday callback rendering is present`); the new horror test and its category-source assertion pass. Per bounded task, npm build, capture, commit and parity-script edits were not run. A direct Astro compiler parse was unavailable because this dependency tree does not export `astro/compiler`; parent build remains the authoritative compile/capture gate.

| 2026-08-11 23:48 | Shared VR game-detail T1196/T121/T396 template | Add the data-gated `sourceParity.kind: "vr-source"` branch only to `/portal-strike/`, `/party-games/`, `/portal-zombie/`, and `/portal-strike-kids/`. Route data owns source record dimensions and the few actual per-route seams (story height/mobile offset, desktop feature height, related-to-party gap, post-venue spacer, and scenario height); `QuestPage` exposes only scoped CSS variables and retains its generic path for every unmarked quest. | R27 raw records establish the common source sequence: T1196 feature rail, T396 copy-only booking, T121 related cards, PartyForm, T396 frosted venue/gallery/callback, then T121 scenarios. Source IDs include `rec1405722081`/`rec1405751401`, `rec1405865811`, `rec1681215121`, `rec1405698711`/`rec1405698721`, `rec1405698781`/`rec1405698791`, `rec1306638331`, and `rec1144740346`; route-specific raw pages and `round-27/visual-detail.json` supply exact 1440/390 boundaries. The branch removes only VR's invented phone/WhatsApp and fallback calendar controls, preserves visible booking copy, does not change comparator code, HolidayPage, or generic quest behavior. Grounded Lazyweb report completed and was opened without degradation: [b69d1118-0685-4e08-9e4c-8cdb671f7e3e](https://www.lazyweb.com/report/lazyweb/b69d1118-0685-4e08-9e4c-8cdb671f7e3e/?source=create). |

- 2026-08-11 23:48 UTC: RED `tests/vr-source-parity.test.mjs` initially failed before source data/branch/style existed; GREEN focused suite `node --test tests/vr-source-parity.test.mjs tests/brawl-source-parity.test.mjs tests/ono-source-blocks-parity.test.mjs tests/zapad-source-parity.test.mjs` passed 11/11. DOM-only Playwright smoke against the existing Astro dev server (no screenshots) matched every tested R27 source top/height at 1440 and 390 for all four routes: story, feature rail, booking, related, PartyForm, venue, gallery, scenarios, callback; zero runtime errors and zero horizontal overflow. Generic `/zvonok/` stayed unmarked and retained both prebooking and booking CTA. Per bounded task, npm build, capture, commit, and comparator edits were not run. Lazyweb's monthly free allowance was reported exhausted after this completed report; no retry or fallback was made.

| 2026-08-11 23:56 UTC | `/prazdniki-pod-kluch/` remaining T829/T396/T662/T827/T1058 tail | Keep the generic Holiday composition untouched and render only `podkluch-closing` through the route-gated `PodKluchClosingArtboard`: two captured five-item collections, all five locally navigable T396 hall slides, the source gallery, and the exact orange T827 review wall. The review wall owns 28 mirrored local source bitmaps at measured 1440/390 masonry coordinates; the T1058 source popup is replaced by the existing local `#prazdnik` form target. | R27 had `px=42.77/28.28` and a −26.39% desktop height delta because its generic tail omitted source venue/package copy and replaced the review record with generic content. Raw `_capture/pages/prazdniki-pod-kluch.json` and live DOM establish `rec685260517`, `rec685281002`, `rec678379442`, `rec678337393/395`, `rec677119223/224`, `rec677119229/230/231`; live T827 after its native in-view state gives the 1360px 3-column desktop and 350px 2-column mobile coordinates. Lazyweb health was available but the provider reported the monthly allowance exhausted at 199/200 and explicitly prohibited retry; no report URL is claimed. |

- 2026-08-11 23:56 UTC: RED `node --test tests/podkluch-closing-artboard-parity.test.mjs` failed before `reviews.items` and the concrete review renderer existed; GREEN focused Pod suite (`podkluch-artboard`, `podkluch-game-grid`, `podkluch-services`, `podkluch-closing`) passed 9/9. The 47 route-local closing assets (including all 28 review bitmaps and the action icon) resolve from `public/`; `git diff --check` is clean. Per bounded task, no npm build, visual capture, commit, or parity-script change was performed.

| 2026-08-12 00:00 UTC | Shared generic QuestPage T396 booking source parity | For QuestPage data with an empty `sourceParity` object, render the captured T396 scheduler-copy record: retain the two stored booking lines, add the source scheduler hint, reserve the measured `300/260px` geometry, and omit clone-only phone/WhatsApp pills. Keep `PrebookingForm` exactly as the existing intentionally masked replacement for the external calendar. The three measured desktop-height exceptions are data-only `booking.sourceHeight` values (`290/260`, `290/260`, `270/260`); source-specific Brawl, Ono, Zapad and VR compositions remain outside this branch. | Raw R27 representative records are `/among_us/ rec674968029`, `/mystery_shack/ rec2004904111`, and `/tekhasskaya-reznya-benzopiloj/ rec1390786441`: each is `300/260`, has the scheduler hint and has no phone/WhatsApp CTA. Across 34 unmarked generic source routes, 31 use 300px desktop; raw `rec704792395` (Garri), `rec674026651` (Kalmar), and `rec672193182` (Beguschij) establish the three captured desktop variants. No new Lazyweb report was attempted: the provider's immediately preceding monthly quota hard-stop explicitly prohibits retry/poll/fallback. |

- 2026-08-12 00:00 UTC: RED `node --test tests/generic-booking-source-parity.test.mjs` first failed while the CSS variables and the three source-height data fields were absent. GREEN focused suite `node --test tests/generic-booking-source-parity.test.mjs tests/vr-source-parity.test.mjs tests/brawl-source-parity.test.mjs tests/ono-source-blocks-parity.test.mjs tests/zapad-source-parity.test.mjs tests/quest-venue-layout-parity.test.mjs` passed 24/24. DOM-only Playwright smoke against the existing Astro server checked the three 300px representatives and all three variants at 1440 and 390: source copy, exact `300/290/270` desktop and `260` mobile heights, no clone-only CTA, no horizontal overflow and no runtime errors. `/brawl_stars/`, `/ono/`, `/kvest_v_realnosti_zapad/`, and `/portal-strike/` were confirmed outside the generic class. Per bounded task, npm build, visual capture, commit, comparator changes and Holiday/artboard edits were not run.

| 2026-08-12 00:13 UTC | Root `/` R27 T604 promo and T395 tab records | Replace the root-only compensating promo margins with the measured source record: desktop `15/30px` record padding around a `1200px` outer slider, `1160×500px` contained image artboard, and its `44px` caption interval; mobile `15/60px`, `390×168.094px` (`1160/500`) artboard, and the same caption interval. Make the source slider arrows and border radii local to `.home-page`. Expose `rec958749021` on the promo and `rec671013302` on the responsive desktop/mobile controls. Reproduce source T395 tab affordances only on the homepage: `#ff6900`, 16px labels, `➧`/`✪` symbols, and the 390px `350×51px` wrapper with its `348×49px` white-text select. | R27 measured `/` at `63.485/48.155` px (1440/390), `1.11/3.33%` height delta, and source/clone section counts `25/19`. Live source inspection fixed the exact anchors: T604 is `650+589` / `570+287.09`; the original image is visually contained rather than stretched, and T395 is `1239+54.3` / `857.09+51`. The old clone simulated the downstream endpoint with `50/24px` pre-margin and `108/118px` post-gap, leaving it as a clone-only `section.promo`; the new source record retains the same visual bitmap endpoint while restoring its actual geometry. The capture-normalized `site.slider` first pair remains Azkaban then Mystery: raw source starts Mystery, but both R27 captures show Mystery after the six-second cycle, so no slide order or readiness code changed. |

| 2026-08-12 00:13 UTC | Lazyweb root `/` | Do not retry or fall back to a new Lazyweb request for this visual-only repair. | The previous provider hard-stop reports its monthly allowance exhausted at `199/200` and explicitly prohibits retry; raw/live Tilda measurements and R27 artifacts are the authority for this bounded change. |

- 2026-08-12 00:13 UTC: RED `node --test tests/home-slider.test.mjs` failed before the T604/T395 record marker and geometry existed. GREEN after the scoped source changes: 9/9 focused tests passed, including the preserved autoplay/data-order contract. No build, visual capture, commit, parity-script change, or Lazyweb retry was performed.

| 2026-08-12 00:50 UTC | Final evidence boundary | Keep R27 as the sole canonical visual matrix and screenshot set. Do not merge partial R28 output into it. Publish the self-contained R27 report together with this non-certificate audit; retain all post-R27 source-backed fixes in the repository with their route-scoped TDD evidence. | R27 is complete (67 routes × 2 viewports × original/clone = 268 screenshots) but fails strict visual thresholds. R28 was interrupted at 238/268 screenshots before `visual-detail.json` and canonical matrix creation. Treating it as a final round would mix incomplete evidence with a different source build. |

| 2026-08-12 01:22 UTC | Desktop mega-menu hover gap | Wrap only mega-menu triggers in `.nav__trigger` and give it a 22px transparent lower hover bridge. The bridge is a descendant of the trigger, so it keeps the existing `li:hover` state while the pointer crosses to the absolutely positioned panel; mobile navigation and menu destinations are unchanged. | Before the fix, the first trigger ended at y=66.5 while the visible panel started at y=86 in the built desktop page, leaving a 19.5px non-hover gap. After a clean build, Chrome smoke moved the pointer through the middle of that interval, verified the panel remained visible and clickable, then followed its first link to `/kvest_v_realnosti_ograblenie_banka_bumazhniy_dom/`. |

| 2026-08-12 01:38 UTC | `/igra-v-kalmara-lend/` source footer labels | Keep the fixed-size Kalmar artboard footer, but replace generic labels with the corresponding live Tilda navigation labels: детский/взрослый/VR день рождения, активные игры, прятки в темноте, страшные квесты, and the full privacy-policy label. | R30 found these source strings absent while the artboard geometry was already exact. Raw `rec866472899` contains the exact labels. A mobile Chrome smoke after build confirms every restored string is rendered, footer height remains 780px, and horizontal overflow remains 0. |
